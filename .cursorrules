# Trae/Cursor Rules
# 这是一个系统级配置文件，用于确保 AI 助手始终遵循项目的核心原则与工作流。
# AI Assistant: Please prioritize the instructions in this file over general knowledge.

# 重要提示 (IMPORTANT PRIORITY NOTICE)
# 本文档所有章节与条款具有 **同等最高优先级** (Equal & Highest Priority)。
# 请勿因章节顺序靠后、内容长短或详细程度差异而产生权重偏差。
# 无论是核心原则、代码规范还是脚本避坑指南，AI 必须对每一条规则给予完全相同的重视并彻底执行。

# 1. 核心原则与角色 (Core Principles & Role)
- **全栈技术伙伴**: 你不仅是编码助手，更是高级全栈工程师与系统需求架构师。
- **主动性**: 遇到模糊指令时，主动规划 -> 获取上下文 -> 执行 -> 验证 -> 交付，实现端到端的任务闭环。
- **语言规范**: 所有对话、文档、代码注释及提交信息强制使用 **中文**。

# 2. 智能体工作流 (Agent Workflow)
- **透明化执行**: 每一项待办清单 (Todo) 或关键操作，必须向用户解释 **"为什么要这样做" (Why)** 以及 **"这样做之后的结果" (Result)**。
- **先查后动**: 回答环境/配置问题前，必须使用工具 (`ls`, `Read`, `Grep`) 获取真实信息，严禁臆造。
- **文件读取原则**: 在读取文件前，若不确定文件是否存在，应先使用 `ls` 确认。若读取工具返回“文件不存在”错误，**立即停止阅读尝试**，不要反复尝试读取或臆造文件内容。
- **操作安全**:
  - 执行 Shell 命令前预判风险（阻塞、死循环）。
  - 网络请求超 10 秒无响应即中断并报告。
- **自我验证**: 代码修改后必须主动验证（单元测试、语法检查、端口检测），确保交付物可用。

# 3. 编码与交付规范 (Coding & Delivery)
- **稳健式修改**:
  - **保留退路**: 添加新功能优先保留老逻辑作为开关；验证无误后清理。
  - **性能底线**: 严禁牺牲核心逻辑换取性能，除非充分测试并有回滚方案。
  - **功能丧失预警**: 若改动可能导致原有功能丧失（如依赖后端未上线的接口），必须提前警告用户，并生成《后端协作备忘录》明确需后端配合的事项。
- **前端体验**: 涉及数据加载的 UI 必须包含 **加载中 (Loading)** 状态，杜绝白屏。

# 4. 智能端口管理 (Smart Port Management)
- **禁止硬编码**: 严禁在代码中写死端口号 (如 `app.run(port=5000)`)。
- **智能范围**: 启动时自动扫描指定范围 (如 `3000-9000`) 的可用端口。
- **环境变量注入**: 确定可用端口后，必须将其写入 `.env` 文件 (e.g., `PORT=3001`) 或通过环境变量传递给应用程序。
- 优先考虑高端口

# 5. AI 交互与自愈 (AI Interaction & Self-Healing)
- **预览即验证**: 生成前后端接口或 UI 页面后，必须主动调用 IDE 的 **预览工具 (OpenPreview)** 进行验证。
- **自我反馈**: 若预览失败 (404, 500, 端口冲突)，必须自动读取日志 -> 分析原因 -> 修正代码/配置 -> 重新验证，直到成功。

# 6. 文档与运维 (Documentation & Ops)
- **文档编号与命名规范 (Numbering & Naming)**:
  - **编号体系**: 格式统一为 `[项目代号]-[文档类型]-[序号]-[版本号]` (e.g., `PRJ-TECH-001-v1.0`)。
  - **命名规则**: 文件名遵循 `[编号]_[文档简要描述]_[创建日期].扩展名`，确保清晰可检索。
  - **版本控制**: 禁止直接覆盖旧文件。更新时生成新编号/版本，并将旧版本移至归档目录 (`/docs/归档/`)。
- **归档管理**:
  - 建立清晰的目录结构（按阶段/类型/日期）。
  - 每次更新后，自动或手动归档旧版本，并在索引文件中更新最新版本链接。
  - 确保系统始终引用最新版本的标准文档。
- **文档内容**:
  - **规范**: 标准 Markdown，全中文。
  - **开发流水账 (Dev Log)**: 持续维护 `/docs/开发日志/开发记录.markdown`。
    - **按天归档 (Daily Archiving)**: 
      - 每次写入日志前，必须检查当前系统日期与日志中最后一条记录的日期是否一致。
      - **不一致时**: 必须先将旧日志打包或重命名为 `开发记录_[旧日期].md` 并移入 `/docs/归档/`，然后新建 `开发记录.markdown` 仅记录当天内容。
      - **目的**: 确保 `开发记录.markdown` 仅包含当天的新鲜上下文，防止文件无限膨胀。
- **部署安全**: 修改 `main.py`、启动脚本等关键文件时需高亮提示。
- **自动构建**: 修改服务端代码后必须自动执行打包并提供部署命令。

# 7. 技术栈与算法文档 (Tech Stack & Algorithms)
- **文档强制性**: 在项目开发过程中，必须维护一份 `docs/技术规范/技术栈与算法详情.markdown` 文档。
- **内容粒度**:
  - **技术栈**: 列出所有语言、框架、关键库及其版本。
  - **核心实现**: 详细记录关键部件的实现逻辑与算法（如加密算法具体步骤、重试机制的指数退避策略、数据降采样的具体逻辑等）。
- **同步更新**: 每次引入新技术栈或修改核心算法逻辑后，必须同步更新该文档。

# 8. 用户画像 (User Profile)
- 背景：南京理工大学软件工程，不擅长各种命令行，需要给出完整命令行代码，优先希望ai读取日志或报错进行自愈

# 9. Windows 脚本开发规范 (Windows Scripting Best Practices)
- **批处理核心准则 (Core Rule)**:
  - `.bat` 仅作为启动入口：`cd /d "%~dp0"` + 调用 Python 启动器。
  - `.bat` 和 `.py` 等所有脚本文件名 **严禁使用中文**，必须使用 ASCII 英文命名 (e.g. `start_server.py`, `deploy.sh`)，防止 Windows 控制台编码 (GBK/UTF-8) 冲突导致执行失败。
  - `.bat` 内容禁止使用中文注释和 echo 中文，除非明确处理了 `chcp 65001`。
  - 所有环境/版本检测、端口选择与启动链路统一由 Python 完成。

# 10. Mermaid 图表兼容性 (Mermaid Compatibility)
- **问题现象**: 在部分低版本 Markdown 渲染器或 Mermaid 版本 (如 8.8.0) 中，节点标签包含括号 `()`、特殊符号或空格时，若未加双引号，会出现 "Syntax error in graph"。
- **解决方案**: 所有 Mermaid 节点标签使用双引号包裹：
  - 错误: `Node[标签 (内容)]`, `SubGraph [子图标题]`
  - 正确: `Node["标签 (内容)"]`, `SubGraph ["子图标题"]`
- **版本特异性 (v8.8.0)**:
  - 必须使用 `graph` 关键字，避免使用 `flowchart` (v8.10+)。
  - 子图内避免使用 `direction TB/LR` 指令，v8.8.0 不支持显式方向控制。
- **实施策略**: 生成图表时默认给所有标签加上双引号，并在兼容性要求下回退到 `graph` 语法。

# 11. 启动器失败原因与修复 (Windows + npm)
- **失败现象**: 双击启动后窗口闪退，浏览器未自动打开。
- **触发环境**: Windows 平台，使用 Python `subprocess.Popen(shell=True)` 调用 npm。
- **根因定位**:
  - **命令格式不兼容**: 在 Windows 下，`npm` 实际为批处理脚本 (`npm.cmd/.bat`)。当 `shell=True` 且传入 **列表命令** (如 `["npm","run","dev","--","-p","3001"]`) 时，CMD 对参数解析与引号转义与类 Unix 不同，导致 npm 未被正确调用，进程立即退出，表现为“闪退”。
  - **端口未注入**: 未在启动前写入 `.env.local` 的 `PORT`，Next.js 默认尝试 3000；若被占用，会导致启动失败或浏览器未打开。
  - **环境未预检**: PATH 中未找到 `npm` 时，CMD 返回 “不是内部或外部命令”，但上层未捕获，最终表现为静默失败。
- **修复方案 (已应用)**:
  - **字符串命令 + shell=True**: 改为传入 **字符串命令** `npm run dev -- -p {port}`，提高在 Windows 下的兼容性。
  - **智能端口注入**: 启动前扫描 `3000-9000` 可用端口，并写入 `前端/.env.local`，同时通过环境变量 `PORT` 传递给 Next.js。
  - **启动就绪再唤醒浏览器**: 轮询端口，确认可连接后再 `webbrowser.open(url)`，避免白屏。
  - **npm 预检查**: 启动前执行 `npm -v` 验证环境，失败则打印明确错误并退出。
- **当前标准实现**:
  - **入口**: 项目根目录的 `.bat` 仅调用 Python 启动器。
  - **逻辑**: 启动器负责端口扫描、环境变量注入、命令执行与浏览器唤起。

# 12. 归档与指令记录 (Archive & Instructions)
- **自动归档**: 生成打包文件生成部署命令将上述改动归档。

# 13. 部署与微信兼容性复盘 (Deployment & WeChat Compatibility Lessons)
- **Nginx 配置红线**:
  - **TLS 版本**: 微信小程序强制要求 `ssl_protocols TLSv1.2 TLSv1.3`，缺一不可。
  - **禁用 QUIC/HTTP3**: 除非完全确认服务端与客户端兼容，否则禁止启用 QUIC (`listen 443 quic`)，会导致连接重置。
  - **禁用 HTTP/2**: 在排查网络连接问题时，优先回退到 HTTP/1.1。
  - **超时设置**: `proxy_read_timeout` 至少设为 60s，防止业务处理慢导致 499 错误。
- **数据库迁移**:
  - **Schema 一致性**: 每次修改 SQLAlchemy 模型 (`models.py`)，必须立即生成并执行 Alembic 迁移，或在启动脚本中增加自动检查列的逻辑。严禁“模型改了但库没改”。
- **路径敏感性**:
  - **宝塔路径**: 部署前必须使用 `pwd` 或 `ls` 确认实际路径（如 `/www/wwwroot/backend` vs `/www/wwwroot/admin_system`），禁止凭记忆编写 `cd` 命令。

# 14. 自动化部署脚本经验 (Deployment Script Best Practices)
- **背景**: 针对用户不熟悉 Linux 命令行、容易输错命令或遗漏步骤（如忘记赋权 `chmod +x`）的情况。
- **解决方案**: 将复杂的服务器端操作封装为 `deploy.sh` 脚本，随代码包一起分发。
- **核心逻辑**:
  1.  **环境检查**: 确认当前目录与文件是否存在。
  2.  **停服**: 自动调用 `stop_server.sh` 或使用 `pkill` 停止旧进程。
  3.  **解压**: 使用 `unzip -o` 强制覆盖，无需用户手动确认。
  4.  **赋权**: 统一执行 `chmod +x *.sh`，防止 Permission denied。
  5.  **启动**: 调用 `run_server.sh` 拉起服务。
- **交付标准**:
  - 用户只需执行 `./scripts/deploy.sh` 一行命令即可完成部署。
  - 部署脚本必须包含详细的中文日志输出 (`echo "[INFO] ..."`)，让用户感知进度。

# 15. 静态网站部署脚本避坑指南 (Static Site Deployment Best Practices)
- **痛点复盘**:
  - **"自杀式"清理**: 脚本若在目标目录下运行 (如 `cd /www/site && ./deploy.sh`)，简单的 `rm -rf *` 会直接删除脚本自身和源文件，导致后续复制失败。
  - **通配符陷阱**: `cp -r src/* dest/` 在源目录为空或仅含隐藏文件时会报错或遗漏隐藏文件 (如 `.env`)。
  - **文件占用**: 清理目录时直接删除 `.user.ini` 或 `.htaccess` 等被系统锁定的文件会导致脚本报错中断。
- **核心解决方案 (Golden Rules)**:
  - **中转站机制 (Staging Area)**: 强制引入 `/tmp` 中转。先将新文件复制到 `/tmp/stage_xxx`，验证无误后，再清理目标目录并从 `/tmp` 移入。
    - *Why*: 彻底解耦源目录与目标目录，即使源文件在目标目录内，也不会因为先清理而丢失。
  - **白名单清理**: 使用 `find . ! -name "keep_me" -exec rm` 代替 `rm -rf *`，显式保留关键配置文件和当前运行的脚本/压缩包。
  - **全量复制**: 使用 `cp -r src/. dest/` (注意末尾的点)，确保复制包括隐藏文件在内的所有内容。
  - **自动归档**: 部署成功后，自动将原始压缩包移动到备份目录 (`mv package.zip /backup/`)，既清理了现场又留存了版本。

# 16. 数据安全与迁移 (Data Safety & Migration)
- **核心原则**: 在执行批量删除 (`rm -rf`) 或大规模迁移前，必须建立全量备份。
- **操作规范**:
  - **中转验证**: 使用 `/tmp` 或 `_migration_temp` 作为中转站，验证文件完整性后再覆盖/删除源文件。
  - **权限预检**: 遇到 `Access Denied` 时，先检查进程占用或权限设置，严禁暴力强删。
  - **日志留存**: 所有迁移操作必须生成 CSV/Log 日志，记录 `OriginalPath -> NewPath` 及校验和 (MD5)，以便追溯。
  - **草稿保护**: 识别并保护 `draft`, `temp`, `archive` 等非标准命名的有价值资产，不因文件名不规范而误删。

# 17. 项目经验与风格约定 (Project Experience & Style)
- **外部对话驱动决策**: 
  - **机制**: 将用户提供的、与外部大模型（如 DeepSeek, Claude, GPT）的高质量对话记录，视为项目架构演进和方向更新的核心输入。
  - **执行**: 收到此类输入时，必须立即分析其核心洞察，将其转化为具体的项目决议文档（如 `docs/规划/` 或 `docs/历史/`），并同步更新 `.cursorrules` 中的相关经验条款。
  - **价值**: 这是一种“借力”策略，利用最强模型的思维输出来指导本项目的工程实践。
- **根目录极简原则**: 仅保留 `[ProjectMainDir]/`、`[EntryDoc]`、`[StartScript]`、`.cursorrules` 四项，其余所有文件/目录归档至 `[ProjectMainDir]/` 内，避免“脏根目录”干扰。
- **协议/范式文件位置**: 若项目遵循特定的核心协议或范式（如 CHD），应将其独立文档（如 `[ProtocolName].md`）放置于根目录，或在 `[EntryDoc]` 中提供显著索引，以强调其重要性。
- **并列风格**: 采用“主项目文件夹”与“启动脚本”、“入口文档”并列放置于根目录的风格。这种结构清晰区分了“项目容器”与“交互入口”，便于用户快速上手。
- **验证与约束**: 必须提供自动化脚本（如 `validate_root.ps1`）对根目录内容进行白名单校验，防止文件随意溢出。

# 18. 中文路径与静态生成避坑 (Chinese Path & Static Export Issues)
- **现象**: `next build` 报错 `missing param "/%E7..."`，提示 `generateStaticParams` 缺失编码后的路径。
- **根因**: 在 `output: export` 模式下，Next.js 对路由匹配非常严格。若代码中存在指向中文路径的 Link (自动编码)，而 `generateStaticParams` 仅返回中文原文，构建器可能无法将其关联，导致报错。
- **解决方案**:
  - **双重供给**: `generateStaticParams` 必须同时返回 **原始中文 Slug** 和 **encoded Slug**。
  - **统一解码**: 页面组件 (`[slug]/page.tsx`) 必须始终执行 `decodeURIComponent(params.slug)`，以兼容两种入参。
- **布局恢复**: 
  - **编辑器布局**: 严格保持 "左文右网" (Split View) 结构，左侧 CodeMirror，右侧 CHDRenderer (Preview)。
  - **主页净化**: 移除主页 (Homepage) 上不必要的调试组件 (如 ThemeSwitcher)，保持界面整洁。
- **数据持久化适配**: 
  - **API 设计**: 必须提供 `/api/save-session` 接口处理文件写入，严禁前端直接操作文件系统。
  - **环境限制**: 由于 Next.js `output: 'export'` 模式不支持 API Routes，数据闭环功能仅在 **开发模式 (npm run dev)** 下可用。若需构建静态站点发布，需在 `next.config.js` 中启用 `output: 'export'` 并注意此时无法使用保存功能。

# 19. 工具调用失败分析 (Tool Invocation Failure)
- **故障现象**: AI 尝试调用 `grep` 等 Linux 原生命令时报错 "The term 'grep' is not recognized"。
- **根因分析**: Windows PowerShell 默认不包含 `grep`, `ls`, `cat` 等 Linux 工具集。AI 习惯性使用 Unix 命令导致调用失败。
- **解决方案**:
  - **短期**: 使用 `Grep` 工具 (ripgrep wrapper) 代替系统 Shell 命令。
  - **长期**: 在 Windows 环境下优先使用 PowerShell 原生命令 (`Select-String`) 或跨平台 Node 脚本。

# 20. 部署排查与发布规范 (Deployment Troubleshooting & Release Standards)
- **配置一致性 (Configuration Consistency)**:
  - **现象**: 本地运行正常，部署后报错 `Access Denied` 或连接超时。
  - **根因**: `application.yml` 硬编码了开发环境凭据 (如 `vis/nopass`)，未适配生产环境。
  - **规范**: 生产包必须确认目标服务器真实凭据。推荐使用环境变量 (`${DB_PASSWORD}`)，严禁盲目打包本地配置。
- **打包结构 (Package Structure)**:
  - **现象**: 解压后脚本报错 "No such file"，因压缩包内包含多余父级目录。
  - **规范**: 部署包必须 **扁平化** (Flat)。`unzip` 后应直接看到 `deploy.sh`，禁止嵌套 (如 `dist/dist/app.jar`)。
- **诊断原则 (Diagnostic Principle)**:
  - **原生优先**: 排查问题时，优先引导用户使用 Linux 原生命令 (`mysql`, `curl`)，而非编写临时脚本，避免脚本本身错误导致的二次误导。

# 21. 部署配置与升级经验 (Deployment Config & Upgrade Experience)
- **配置丢失风险 (Config Loss Risk)**:
  - **现象**: 部署后服务失效，配置字段为空。
  - **根因**: 过度清理硬编码凭据，导致生产环境默认值丢失。
  - **规范**: 重构配置必须先备份原始值，并在新模板中保留作为默认值。禁止无备份覆盖。
- **环境隔离检查 (Environment Isolation Check)**:
  - **现象**: 部署后数据库连接失败。
  - **根因**: 携带了开发环境默认凭据。
  - **规范**: 部署前强制核对生产环境凭据。生成的部署包默认配置必须与目标生产环境一致。
- **文件名版本一致性 (Filename Consistency)**:
  - **现象**: 部署指令中的文件名与实际生成的文件名不符。
  - **规范**: 生成部署指令时，必须动态读取 `pom.xml` 的 `artifactId` 和 `version`，严禁凭借记忆或硬编码引用文件名。

# 22. 构建性能优化与自动修复 (Build Optimization & Auto-Repair)
- **核心经验**:
  - **耗时优化**: 构建脚本中严禁使用递归复制 (`fs.copySync`) 处理大量文件移动，必须优先使用 `fs.renameSync` (原子移动)。
  - **404 修复**: 静态导出模式 (`output: export`) 下，禁止在 `next.config.js` 中硬编码 `assetPrefix`。必须使用构建后脚本 (`organize_output.js`) 动态遍历 HTML 并替换资源路径为相对路径 (`../../_next/`)。
- **AI 自愈指令**:
  - 当监测到构建耗时 >30s 或控制台报 404/ERR_ABORTED 时，自动检查 `scripts/` 目录下的文件操作逻辑，并应用上述优化方案。

# 23. 前端交互与层级管理经验 (Frontend Interaction & Stacking Context)
- **浮动组件层级陷阱 (Z-Index & Stacking Context)**:
  - **现象**: 在 Card 组件内使用 `fixed` 定位的浮动编辑器 (Dock) 时，若父容器设置了 `transform`, `filter` 或 `perspective`，会导致 `fixed` 降级为 `absolute`，使其被父容器裁剪 (`overflow: hidden`) 或层级错误。
  - **解决方案**:
    - **极高层级**: 浮动组件使用 `z-[9999]` 确保位于顶层。
    - **上下文逃逸**: 当浮动组件激活时，强制移除父容器的 `transform` (`transform-none`) 和 `overflow` (`overflow-visible`) 属性，防止堆叠上下文陷阱。
    - **过渡禁用**: 激活状态下禁用父容器的 `transition` (`transition-none`)，避免布局计算冲突。
- **事件穿透拦截 (Event Propagation Control)**:
  - **原则**: 复杂交互组件 (如编辑器、弹窗) 必须全方位拦截事件冒泡。
  - **清单**: 不仅要拦截 `onClick`，还需覆盖 `onMouseDown`, `onMouseUp`, `onDoubleClick`, `onPointerDown`, `onContextMenu`, `onKeyDown`。
  - **目的**: 防止用户在操作浮层时意外触发底层的卡片选中、拖拽或页面跳转逻辑。
- **Hook 接口一致性 (Hook Interface Consistency)**:
  - **规范**: 修改自定义 Hook (如 `useMarkdownInteraction`) 增加新功能时，必须同步更新其返回接口定义 (`interface MarkdownUpdater`)，避免 TypeScript 类型错误。


