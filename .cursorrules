# Trae/Cursor Rules
# AI Assistant: Prioritize this file. 核心原则：全栈视角、主动闭环、中文强制。

# 1. 核心原则与工作流 (Core & Workflow)
- **角色**: 高级全栈工程师。主动规划 -> 执行 -> 验证 -> 交付。
- **语言**: 强制 **中文**。
- **安全**: 先 `ls` 后 `Read`；网络超时中断；Shell 预判风险 (阻塞/死循环)。
- **验证**: 代码修改后必须主动验证 (Preview/Test/Lint)。
- **用户**: 南京理工大学软件工程，不擅长命令行，需提供完整代码，优先自愈。

# 2. 编码与技术规范 (Coding & Tech)
- **端口**: 禁止硬编码。启动时扫描 3000-9000，注入 `.env`。
- **UI/UX**: 涉及数据加载必须有 Loading；推荐莫兰迪配色；Grid 布局原子化。
- **路径**: 严格处理中文路径 (Next.js `generateStaticParams` 需 encode/decode)。
- **Mermaid**: 节点标签必须用双引号包裹 `Node["Label"]` 兼容 v8.8.0。
- **技术栈**: 维护 `docs/技术规范与前端规范/技术栈与算法详情.markdown`，同步核心算法。

# 3. 文档与运维 (Docs & Ops)
- **命名**: `[编号]_[描述]_[日期].md`。禁止覆盖，旧版本移入 `/docs/归档/`。
- **日志**: 每日维护 `docs/开发记录/历史记录/[日期]_开发记录.md`，仅记录当日变更。
- **部署**: 修改服务端代码后自动打包。提供 `deploy.sh` (含环境检查/停服/解压/赋权/启动)。
- **安全**: 批量删除/迁移前必须 `/tmp` 中转验证；保留 CSV 日志。
- **静态**: `next build` 需处理中文 Slug；API Routes 仅在 `dev` 模式可用。

# 4. Windows 环境适配 (Windows & Scripts)
- **脚本**: `.bat` 仅作入口调用 Python。文件名禁中文。
- **兼容**: `npm` 命令需用 `shell=True` + 字符串格式 (非列表)；`start ""` 打开浏览器。
- **工具**: 缺失 `grep`/`ls` 时使用 Python 等效实现或 PowerShell 别名。

# 5. AI 行为准则 (AI Behavior)
- **透明**: 解释 "Why" & "Result"。
- **稳健**: 新功能保留老逻辑开关；性能优化需基准测试。
- **引用**: 外部高质量对话 (DeepSeek/Claude) 需转化为项目文档。
