# Git 游戏化操作指南

> **核心理念**: 
> * **Git 仓库** = 你的游戏存档文件夹
> * **Commit (提交)** = 存档点 (Save Point)
> * **Branch (分支)** = 开启一条支线任务 (Side Quest)

---

## 1. 每日登录 (开始工作前)

### 回到主线
*   **触发时机**: 每天开始写代码前，或者结束一个功能分支后。
*   **作用**: 切换回主分支（你的通关存档），确保你站在最新的起点上。
    ```bash
    git checkout master
    ```

### 获取更新
*   **触发时机**: 刚切换回 master 分支后。
*   **作用**: 如果有远程仓库（如 GitHub），拉取队友或自己之前的最新代码。
    ```bash
    git pull origin master
    ```

---

## 2. 开启支线任务 (开发新功能)

### 创建并切换分支
*   **触发时机**: 准备开发一个新功能（例如“Markdown表格支持”）时。**绝对不要在 master 上直接改！**
*   **作用**: 创建一个平行世界（分支），你在里面的任何修改都不会影响主线。
    ```bash
    git checkout -b feature/你的功能名
    # 例如: git checkout -b feature/table-support
    ```

### 查看当前分支
*   **触发时机**: 忘记自己在哪条时间线时。
*   **作用**: 列出所有分支，带 `*` 号的是当前所在分支。
    ```bash
    git branch
    ```

---

## 3. 存档 (保存进度)

### 查看修改状态
*   **触发时机**: 写了一会儿代码，想看看改了哪些文件。
*   **作用**: 列出工作区变化，红色表示未暂存，绿色表示已暂存。
    ```bash
    git status
    ```

### 放入背包 (暂存)
*   **触发时机**: 确认修改无误，准备存档前。
*   **作用**: 把所有修改放入“暂存区”，标记为下一次存档的内容。
    ```bash
    git add .
    ```

### 正式存档 (提交)
*   **触发时机**: 完成了一个小步骤（如“写好了正则”），或者要下班了。
*   **作用**: 生成一个永久的存档点。`-m` 后面必须写清楚这次存了什么。
    ```bash
    git commit -m "feat: 这里写你做了什么"
    # 例如: git commit -m "feat: 完成表格正则匹配"
    ```

---

## 4. 任务结算 (合并回主线)

### 切回主线
*   **触发时机**: 支线功能开发完毕，测试通过后。
*   **作用**: 准备接收支线成果，先回到主分支。
    ```bash
    git checkout master
    ```

### 合并支线
*   **触发时机**: 已经在 master 分支上。
*   **作用**: 把支线剧情合并进主线。
    ```bash
    git merge feature/你的功能名
    # 例如: git merge feature/table-support
    ```

### 删除支线
*   **触发时机**: 合并成功后。
*   **作用**: 清理不再需要的支线，保持清爽。
    ```bash
    git branch -d feature/你的功能名
    ```

---

## 5. 读档与后悔药 (救命操作)

### 放弃未提交修改
*   **触发时机**: 刚改乱了文件，还没执行 `git add` 或 `git commit`。
*   **作用**: **慎用！** 丢弃当前所有修改，一键还原到上次存档状态。
    ```bash
    git checkout .
    ```

### 撤销最近一次提交 (保留代码)
*   **触发时机**: 刚执行了 `git commit`，但发现描述写错了，或者漏提了文件。
*   **作用**: 撤销最近一次存档记录，但**保留你写的代码**在工作区，让你修正后重新存。
    ```bash
    git reset --soft HEAD~1
    ```

### 强制回退 (重读档)
*   **触发时机**: 彻底搞砸了，想回到过去的某个版本，后面的修改都不要了。
*   **作用**: **危险！** 彻底回退到指定版本。先用 `git log` 查 ID。
    ```bash
    git reset --hard <CommitID>
    ```

---

## 6. 常用指令速查

### 初始化仓库 (开新局)
*   **触发时机**: 项目刚开始时（只需一次）。
    ```bash
    git init
    ```

### 查看历史 (通关记录)
*   **触发时机**: 想回顾之前的存档点时。
    ```bash
    git log --oneline
    ```
