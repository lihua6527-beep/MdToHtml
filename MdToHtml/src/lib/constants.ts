/**
 * Global Constants for File System and Application Configuration
 */

// File Extensions
export const MARKDOWN_EXTENSIONS = ['md', 'markdown'];
export const MARKDOWN_EXT_RE = /\.(md|markdown)$/i;

// Cache Configuration
export const CACHE_FILE_NAME = '.metadata_cache.json';
export const CACHE_VERSION = '2.0';
export const DEFAULT_CAPACITY = 500;

// Trash Configuration
export const TRASH_DIR_NAME = '.trash';
export const TRASH_RETENTION_DAYS = 30; // Future use

// Pagination
export const DEFAULT_PAGE_SIZE = 20;

// Date Formats (if needed)
export const DATE_FORMAT = 'YYYY-MM-DD HH:mm:ss';
