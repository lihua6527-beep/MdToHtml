# AI 数据支持交付清单

## 1. 修改文件清单
- **前端页面**: `src/app/editor/page.tsx`
    - 增加 `lastSavedContent` 状态追踪。
    - 修改 `handleSaveToWorkspace` 函数，发送修改前后的内容对比。
- **后端接口**: `src/app/api/dataset/route.ts`
    - 升级数据存储逻辑，支持 `previous_content` 和 `current_content` 字段。
    - 优化数据存储格式，增加 `task_type` 字段区分任务类型。

## 2. 新增文档
- `docs/技术方案/AI数据支持方案.md`
- `docs/交付清单/AI数据支持交付清单.md`

## 3. 验证标准
1.  **功能验证**:
    - 打开现有文档，进行修改。
    - 点击保存。
    - 检查 `data/training_dataset.jsonl` 文件，确认新增了一条记录。
    - 确认记录中包含 `input` (修改前) 和 `output` (修改后) 内容，且内容正确。
2.  **回归验证**:
    - 确认文档保存功能本身正常（文件被正确覆盖写入 `input/` 目录）。
    - 确认无控制台报错。
