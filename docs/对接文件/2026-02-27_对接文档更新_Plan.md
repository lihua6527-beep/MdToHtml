# 2026-02-27_对接文档更新_Plan

## 目标 (Objectives)
更新 `docs/对接文件/[003]_AI对接综合指南_20260227.md`，以解决“文件名规范”与“实际交付中文文件名”之间的冲突。明确支持中文文件名，并为下游系统提供技术指导（URL Encoding）。同时整理并提供样例文件。

## 涉及文件 (Affected Files)
1.  `docs/对接文件/[003]_AI对接综合指南_20260227.md` (修改)
2.  `public/static_pages_example/` (新建目录)
3.  `public/static_pages_example/卷积神经网络研究综述.json` (新建/迁移)
4.  `public/static_pages_example/卷积神经网络研究综述.html` (新建/迁移)

## 实施步骤 (Implementation Steps)

### 1. 建立样例目录与文件
-   在 `public/` 下创建 `static_pages_example` 目录。
-   创建 `卷积神经网络研究综述.json`，内容符合元数据规范。
-   创建 `卷积神经网络研究综述.html`，内容为一个简单的 HTML 骨架（或从现有输出复制）。

### 2. 更新对接文档
-   **修改第 3 节 (交付目录结构)**:
    -   废除“严格遵循 kebab-case”的限制。
    -   明确允许中文文件名（Slug），以保持与原 Markdown 标题的一致性。
    -   添加“风险提示”：中文路径在 Web 服务中可能遇到编码问题。
-   **新增/修改技术指导**:
    -   在“文件对应与分流策略”或新增章节中，说明如何处理中文文件名。
    -   **关键点**:
        -   文件系统读取时通常支持 UTF-8，无须特殊处理。
        -   **URL 访问/路由时**：必须使用 `encodeURIComponent(slug)`。
        -   示例代码 (TypeScript/Node.js): 展示如何正确生成指向中文 HTML 的链接。

### 3. 验证
-   人工检查文档描述是否清晰。
-   确认样例文件存在且符合描述。

## 回滚计划 (Rollback Plan)
-   如果用户不满意，通过 Git 撤销对文档的修改。
-   删除 `public/static_pages_example` 目录。
