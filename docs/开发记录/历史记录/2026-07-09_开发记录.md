# 2026-07-09 开发记录

## 工作内容：文章四分类功能全面移除

### 背景

根据用户决策，彻底移除「文章四分类」（项目/论文/知识/其他）功能，该功能从未正确工作（字段命名不匹配），且实际只用于在侧边栏显示一个彩色标签，维护成本虽低但增加了系统的认知负担。决定全面、干净地删除所有相关代码和文档中的分类引用。

### 涉及文件及修改

| # | 文件 | 修改内容 |
|---|------|---------|
| 1 | `plans/文章四分类功能审视与评估报告_20260709.md` | → 归档至 `docs/归档/计划书/` |
| 2 | `DocumentItem.tsx` | 删除 `normalizeType`、`typeLabels`、`typeColors` 及所有标签渲染 |
| 3 | `cache-manager.ts` | 删除 `scanAndSync` 和 `update` 中的 `type` 字段 |
| 4 | `posts.ts` | 删除 `getAllPosts` 中的 `type: entry.type` |
| 5 | `PromptEngine.ts` | 删除 Prompt A/B 中的 `category` 字段及可选字段说明 |
| 6 | `ExportButton.tsx` | 删除 `metadata.type` 字段 |
| 7 | `save-export/route.ts` | 删除分类子目录创建 + 基于 type 的路由逻辑 |
| 8 | `useDocumentState.ts` | 删除 `documentType`、`showTypeDropdown`、`handleTypeChange` |
| 9 | `editor/page.tsx` | 删除 metadata 中的 `type: frontmatter.type` |
| 10 | `InteractivePost.tsx` | 删除 `documentType`、`showTypeDropdown` 等所有引用 |
| 11 | `NavigationHeader.tsx` | 删除类型标签按钮、下拉菜单、`typeLabels`/`typeColors` |
| 12 | `CHD协议.md` | 删除 Frontmatter 中的 `category` 字段示例 |
| 13 | `docs/核心规划/5_CHD协议与AI生成指南.md` | 同上 |
| 14 | `docs/归档/AI服务层扩展计划_v1/CHD_System_Prompt_A/B` | 标记为 `.bak`（归档旧版本） |

### 验证结果

- `npx next build --no-lint` ✅ 编译通过
- ✅ 类型检查通过
- ✅ 86 个静态页面全部生成成功
- ✅ 导出 API 路径简化（取消分类子文件夹）
- ✅ 导航栏和文档列表不再显示分类标签