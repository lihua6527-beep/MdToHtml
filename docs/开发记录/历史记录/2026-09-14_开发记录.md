# 开发记录 — 2026-09-14

## 今日工作摘要

### 一、自动化测试与 CI 门禁修复（P0–P5，已落库）✅

依据 `plans/测试工程/00_CI全绿计划书_2026-09-14.md` 执行，提交：`a0db033`（89 文件，+7148/−4259）+ `424be17`（计划书回填）。

| 子任务 | 文件 | 说明 |
|-------|------|------|
| 7 月成果落库 | `src/types/operation.ts`、`lib/OperationBuilder.ts`、`lib/AIContextBuilder.ts`、`components/CheckpointPanel.tsx`、`AIInline*.tsx` 等 | 此前为 untracked，导致 CI 找不到被测模块 |
| Jest 只管单测 | `MdToHtml/jest.config.js` | 补 `testPathIgnorePatterns: /e2e/`，收敛 `testMatch`；修复 3 个 Playwright spec 被误收 |
| 覆盖率全量口径 | 同上 | 新增 `collectCoverageFrom: src/**`（此前分母只含"触达文件"，数字虚高至 41%） |
| 覆盖率阈值门禁 | 同上 | `coverageThreshold` 19/14/13/20（= `floor(实测)−1`，防回退） |
| npm 脚本恢复 | `MdToHtml/package.json` | `test:e2e` / `test:all` / `typecheck` / `lint:strict` / `verify` |
| E2E 数据隔离 | `playwright.config.ts` + `config.e2e.json` + `lib/config-manager.ts` | `MDTOHTML_CONFIG` 开关 → 读写全部落 `.e2e-tmp/`；`reuseExistingServer: false` |
| 过期断言修复 | `src/lib/__tests__/operation.test.ts`、`src/lib/export/HtmlBundler.test.tsx` | 操作类型 14→13；改用 `getCleanCSS` 主路径 mock + 新增降级用例 |
| 转义失效修复 | `src/lib/export/HtmlBundler.tsx`、`getCleanCSS.ts` | `escapeTemplate`/`escapeAttr` 原为空操作，补 `&amp; &lt; &gt; &quot;` |
| E2E 用例重写 | `e2e/*.spec.ts` | 按重构后的真实 UI 重写为 7 个用例；新增 `data-testid`：`doc-item`/`trash-item`/`trash-restore` |
| 清理误跟踪产物 | 索引操作 + 根 `.gitignore` | `playwright-report/`、`test-results/`、`__pycache__` 移出索引；补 `.e2e-tmp/` |
| 删除废弃 workflow | `MdToHtml/.github/workflows/ci.yml` | 嵌套位置的 Python 版流水线，GitHub 永不执行 |
| 文档对齐 | `MdToHtml/tests/README.md` | 重写为三层测试体系说明（原版描述"浏览器测试页 + Python 脚本"，已过时） |

### 二、本次新增/修正文档 ✅

| 子任务 | 文件 | 说明 |
|-------|------|------|
| 完成度分析报告 | `docs/自动化测试与CI流水线_完成度分析报告_2026-09-14.md` | 新建：资产清单、完成度矩阵（对照 3 份计划书）、实测数据、G1–G9 缺口、P6–P8 路线 |
| 测试与 CI 门禁规范 | `docs/开发工作流/SOP_测试与CI门禁规范.md` | 新建：命令速查、四道门禁、单测/E2E 规范、覆盖率阈值规则、故障排查决策树 |
| 缺口补全计划 | `plans/测试工程/01_CI缺口补全计划_2026-09-14.md` | 新建：P6 运维闭环 / P7 覆盖率爬坡 / P8 质量看板（含任务分解与验收标准） |
| README 修正 | `README.md` §🧪 测试覆盖 | 删除"行覆盖率 ≥80%/分支 ≥70%"等与事实不符的门禁描述，替换为真实规模与阈值 |
| 系统索引修正 | `PROJECT_SYSTEM_INDEX.md` | ① `lib/__tests__` 4→10 文件；② `tests/` 标注为历史遗留；③ 新增 `e2e/`；④ 新增「十七、测试与 CI 门禁」 |

---

## 实测结果（2026-09-14）

| 命令 | 结果 | 耗时 |
|------|------|:----:|
| `npm run typecheck` | ✅ 0 错误（EXITCODE=0） | ~10s |
| `npm run lint:strict` | ✅ `No ESLint warnings or errors`（EXITCODE=0） | ~15s |
| `npm run test:ci` | ✅ 25 套件 / 213 用例通过（EXITCODE=0） | 6.0s |
| 覆盖率（全量 src 口径） | Stmts 20.5 / Branch 15.82 / Funcs 14.03 / Lines 21.25（≥ 阈值 19/14/13/20） | — |
| `npx playwright test` | ✅ 7 passed（EXITCODE=0） | 22.3s |
| 数据隔离复测 | ✅ `input/` 仍 27 篇；`config.json` mtime 仍为 2026-09-07 20:13:38；改动只出现在 `.e2e-tmp/` | — |

---

## API 变化

无。本次仅涉及测试配置、测试用例与文档，未改动任何 API 路由。

---

## 索引变化

| 文件 | 变化 |
|------|------|
| `PROJECT_SYSTEM_INDEX.md` | 头部补「最后更新」；目录树中 `lib/__tests__/` 4→10 文件、`tests/` 改为历史遗留说明、新增 `e2e/` 条目；`lib/__tests__/` 清单补全为 10 个文件并列出其余 5 处单测目录；新增第十七章「测试与 CI 门禁」 |
| `docs/开发记录/开发记录索引.md` | 新增「2026年9月」段落，挂链本记录 |
| `README.md` | §🧪 测试覆盖 整段重写 |
| `MdToHtml/tests/README.md` | （由 P5 完成，本日） |

---

## 遗留与待裁决

| 项 | 状态 |
|----|------|
| 仓库无 git remote → CI 从未真实触发 | 🔴 未解决（计划书 P4-4.5）。2026-09-14 已配置 Gitee 远端，但**平台不匹配**（Gitee 不跑 GitHub Actions）且分支**分叉 2/2**；平台方案见 `plans/测试工程/01_CI缺口补全计划_2026-09-14.md` §6.0 |
| `release.yml` 发布流水线缺失 | 🟠 未实现（P6） |
| `cache-manager.ts`（P0 模块）无专属测试，覆盖率 23.66% | 🟠 未实现（P7） |
| Codecov / README 徽章 / CI 失败通知 | 🟡 未实现（P8） |
| 覆盖率 20.5% 距计划 ≥60% 目标 | 🟡 未达成（P7 分档爬坡） |
| 工作区残留：根 87B `package-lock.json`、`MdToHtml/.trash/演示报告_*.md`（4 个） | ⏸ 待用户裁决（`plans/审计与` 已于本次归档处理） |

---

## 后续归档记录（2026-09-14）

**改动摘要**：过时文档全量归档（14 项）+ 全部跨文档引用同步修正 + 配置 Gitee 远端并查明分支分叉。

### 归档清单

| 原路径 | 新位置 | 归档理由 |
|--------|--------|----------|
| `plans/CI_CD流水线/`（00/01/02，3 份） | `docs/归档/计划书/CI_CD流水线/` | 07-12 四阶段计划，已被 `plans/测试工程/00`（P0–P5 执行结果）+ `01`（P6–P8 缺口）与 09-14 报告取代；02 号"待完成清单"已全部完成或失效 |
| `plans/集成测试/`（00/01/02，3 份） | `docs/归档/计划书/集成测试/` | 测试层次策略与覆盖率排期已并入 09-14 报告 §3.3 与 `01_CI缺口补全计划` §三；02 号"6 个用例全失败"已失效（现 7/7 通过） |
| `plans/操作引擎/04_AI上下文与成本控制策略_2026-07-12.md` | `docs/归档/计划书/操作引擎/` | 与下一份重复编号，策略已由后者与 `AIContextBuilder.ts` 落地 |
| `plans/操作引擎/04_上下文构建器与成本控制_2026-07-12.md` | 同上 | `AIContextBuilder.ts`（6890 B）已实现并在 `InteractivePost` 链路使用 |
| `plans/操作引擎/05_编辑器内AI内联辅助编辑_2026-07-12.md` | 同上 | `AIInlineToolbar.tsx` + `AIInlineResultDialog.tsx` 已实现并集成（`InteractivePost.tsx` 内 14 处引用） |
| `plans/对话上下文_2026-07-25_至_2026-07-26.md` | `docs/归档/计划书/` | 07-25/26 对话交接文档，其"未完成清单"中的 04/05 项现已完成 |
| `plans/审计与`（文件名乱码） | `docs/归档/计划书/计划审计与下一阶段优先级报告_2026-07-25.md` | 07-25 审计报告，结论已被 09-14 报告取代；同时修正乱码文件名 |
| `docs/EXE打包可行性分析报告.md` | `docs/归档/` | 决策已落地（便携版 EXE 已产出，`npm run build:electron`） |
| `docs/删除electron冗余代码安全性分析.md` | `docs/归档/` | 分析结论已执行（commit `308b066` 移除 server.js / path-manager.js） |
| `MdToHtml/tests/{integration_test.py, integration/, unit/}` | `MdToHtml/archive/legacy_python_tests_2026-09-14/` | 早期 Python 测试，与 TS 技术栈无关、不在 CI 运行（对应报告缺口 G9） |

**未归档（仍有效）**：`plans/测试工程/00`、`plans/测试工程/01`、`plans/操作引擎/07_拓展其他生产者_2026-07-12.md`（Phase 5 未来扩展，未实现）、`plans/next_phase_AI内联编辑实施计划_2026-07-26.md`（二次迭代待优化项仍存在，本次仅修正其"14 种"→"13 种"的事实错误）。

### 远端配置与分支分叉（重要发现）

- 配置 `origin = https://gitee.com/njustzjh/md-to-html.git`（凭据可用，`git fetch origin --prune` 成功拉取全部分支）。
- **平台不匹配**：远端是 Gitee，**不执行** `.github/workflows/*.yml`（GitHub Actions）→ 仅配置 remote 无法让 CI 变绿。
- **分叉 2 / 2**：远端 `feat/exe-package-ready` 独有 `d249e43`（07-26 工作区快照）、`01bc1ce`（README 克隆说明）；本地独有 `a0db033`、`424be17`（含全部 09-14 测试/门禁修复）。直接 push 被拒；**未做 force push**，远端提交完好。
- 内容比对：`git diff a0db033 d249e43` = 47 个文件差异（远端缺 `config.e2e.json`、仍是旧 `jest.config.js` 口径与嵌套 workflow）→ 不可"采用远端版本"。

### 同步修正的引用（归档后路径全部有效）

| 文件 | 修正点 |
|------|--------|
| `docs/开发工作流/SOP_测试与CI门禁规范.md` | §八 文档表；§6.1 决策树新增"平台匹配""push 被拒/分叉"两步 |
| `docs/自动化测试与CI流水线_完成度分析报告_2026-09-14.md` | 头部对照文档、§1 ⑤远端、§5（G1/G8/G9）、§5.1 全面重写、§7.3 索引表 |
| `plans/测试工程/00_CI全绿计划书_2026-09-14.md` | 关联文档指向归档位置 |
| `plans/测试工程/01_CI缺口补全计划_2026-09-14.md` | 上游计划/依据引用；**新增 §6.0 Gitee 平台适配与三方案（A/B/C）** |
| `PROJECT_SYSTEM_INDEX.md` | 目录树 `tests/`、`archive/`；§17.2 远端说明 |
| `MdToHtml/tests/README.md` | §5 历史遗留 → 标注归档位置 |

> **流程说明**：`scripts/archive_and_record.py` 每次只接受单个 `--plan`，批量归档 14 项会产生 14 段重复记录与重复索引条目，故本次改用**文件系统移动（git 自动识别重命名）**，并在开发记录中合并为一段如实记录。

- **归档时间**: 2026-09-14

---

> **关联计划书**：`plans/测试工程/00_CI全绿计划书_2026-09-14.md`、`plans/测试工程/01_CI缺口补全计划_2026-09-14.md`
> **关联报告**：`docs/自动化测试与CI流水线_完成度分析报告_2026-09-14.md`
> **关联手册**：`docs/开发工作流/SOP_测试与CI门禁规范.md`
