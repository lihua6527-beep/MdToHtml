
## 2026-02-09: 交互增强、属性扩展与协议升级

### 1. 核心变动
- **交互与 UI 优化**:
  - **布局控制**: 新增 "+" 按钮与布局切换器，支持卡片列数与占宽调整。
  - **悬浮属性面板**: 实现非依附式底部属性面板，支持样式切换、字体大小与对齐调整。
  - **中文本地化**: 卡片样式名称全面汉化。
- **协议升级 (v1.2)**:
  - **文本排版支持**: 协议新增 `title-align`, `content-align`, `title-size`, `content-size` 属性。
  - **默认居中**: 所有卡片标题默认居中对齐。
- **解析增强**:
  - **Highlights 优化**: 空内容自动隐藏，删除空摘要章节。
  - **通用属性解析**: 确认解析器支持通用 KV 属性透传。

### 2. 统计信息
- **修改文件**: `Card.tsx`, `Section.tsx`, `CHDRenderer.tsx`, `CHD协议规范_v1.2.md` (原 v1.1).

## 2026-02-08: 数学公式支持、乱码治理与协议升级

### 1. 核心变动
- **复杂数学公式渲染 (LaTeX Math Support)**:
  - **技术栈集成**: 在 `Card.tsx` 的 ReactMarkdown 管道中集成 `remark-math` (解析) 和 `rehype-katex` (渲染)。
  - **样式引入**: 全局引入 Katex CSS，确保数学符号渲染精确。
- **乱码根源治理 (Garbage Character Fix)**:
  - **输入端**: 修复 Markdown 源文件，将 HTML 实体 (`&gt;`, `&lt;`) 替换为原始符号 (`>`, `<`)。
  - **渲染端**: `CHDRenderer.tsx` 新增预处理层，自动解码实体字符，构建乱码防御网。
- **排版与协议优化**:
  - **加粗渲染修复**: 确立 "中文加粗需空格隔离" 规则，修复 `**` 紧邻文本导致的解析失效问题。
  - **宽卡片布局**: 优化 `col-span >= 2` 的卡片布局，标题强制居中，内容限制最大宽度并保持左对齐，提升阅读体验。
  - **协议升级**: 更新 `CHD协议规范_v1.2.md` (v1.1 -> v1.2)，明确禁止 HTML 实体与紧凑加粗写法。

### 2. 统计信息
- **修改文件**: `Card.tsx`, `CHDRenderer.tsx`, `CHD协议规范_v1.2.md`, `input/*.md`.
- **新增依赖**: `remark-math`, `rehype-katex`, `katex`.

## 2026-02-07: 下沉式交互、构建优化与Undo机制

### 1. 核心变动
- **下沉式智能交互 (Sinking Interactive Editing)**:
  - **交互重构**: 将编辑权下放到 `InteractivePost` 组件，实现“点击即选中”的精准交互。
  - **状态隔离**: 仅当选中卡片时显示编辑工具栏，避免界面杂乱。
  - **精准标题编辑**: 实现双击/按钮触发标题编辑，保留 YAML 元数据不变。
- **历史记录与撤销 (History & Undo)**:
  - **栈式管理**: 在 `useMarkdownInteraction` 中实现 `historyStack`。
  - **操作回滚**: 支持删除、添加、标题修改的无损撤销，配合左上角 Undo 按钮提供安全感。
- **构建性能与报错修复 (Build Optimization)**:
  - **性能飞跃**: 重构 `post-build.js`，使用 `fs.renameSync` (移动) 替代递归复制，显著减少 IO 耗时。
  - **404 终结**: 移除 `next.config.js` 的 `assetPrefix`，改用 `organize_output.js` 动态计算并替换 HTML 中的资源路径 (`src="/_next/..."` -> relative)，完美解决静态导出路径错乱问题。
- **数据闭环 (Dataset Loop)**:
  - 完善 `/api/dataset`，在保存时自动记录 Input/Output 对齐数据至外部文件。

### 2. 数据闭环升级 (Phase 2)
- **全量会话归档**: 新增 `/api/save-session`，实现 Input(初始) + Output(最终) + Operations(操作流) 的原子化归档。
- **操作日志**: `useMarkdownInteraction` 新增动作捕捉，记录每一次增删改移的时间戳与参数，为行为分析提供数据支持。
- **存储规范**: 数据统一落盘至项目根目录 `data/{slug}_{timestamp}/`，确保中文路径兼容。

### 3. 界面与体验优化
- **主题显性化**: 编辑页顶部集成 `ThemeSwitcher`，并增加中文提示，支持实时切换配色。
- **交互入口**: 强化编辑入口可见性，保持界面整洁的同时降低认知负荷。

### 4. 统计信息
- **新增文件**: `scripts/organize_output.js`, `src/components/__tests__/InteractivePost.test.tsx`, `src/app/api/save-session/route.ts`.
- **修改文件**: `InteractivePost.tsx`, `useMarkdownInteraction.ts`, `post-build.js`, `next.config.js`, `ThemeSwitcher.tsx`, `.cursorrules`.
- **测试**: 7 个测试套件全部通过 (100% Pass).
