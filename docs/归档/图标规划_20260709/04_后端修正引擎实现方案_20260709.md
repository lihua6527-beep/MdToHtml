# 阶段二：后端修正引擎实现方案

> **关联阶段**: 阶段二（P0）
> **预计工时**: 2h
> **涉及文件**: IconAutoFixService.ts（新建）/ CHDRenderer.tsx
> **前置依赖**: 阶段一（需要 icon-manager.ts 新增 iconExists 等函数就绪）

---

## 一、概览

在 AI 生成的 Markdown 被解析后、渲染之前，增加一层图标自动修正引擎。这是全链路容错架构的核心组件，负责拦截并自动修复 AI 的图标错误。

## 二、步骤 2.1：创建 IconAutoFixService.ts（1.5h）

### 2.1 文件位置

`MdToHtml/src/services/ai/IconAutoFixService.ts`

### 2.2 核心数据模型

```typescript
import { iconExists } from '../../lib/icon-manager';

// 单张卡片的图标信息
export interface CardIconInfo {
    blockIndex: number;         // 卡片在 blocks 数组中的索引
    iconName: string | null;    // 当前图标名（null = 无图标）
    isValid: boolean;           // 图标名是否在 icon-manager 中有效
}

// 单个 Section 的图标信息
export interface SectionIconInfo {
    sectionTitle: string;       // Section 标题（用于日志）
    cards: CardIconInfo[];      // 该 Section 下所有卡片的图标信息
}

// 单次修正记录
export interface FixRecord {
    cardIndex: number;          // 被修正的卡片索引
    from: string | null;        // 修正前的图标名（null = 无图标）
    to: string | null;          // 修正后的图标名（null = 清除图标）
    reason: string;             // 修正原因
}

// 一个 Section 的修正报告
export interface SectionFixLog {
    sectionTitle: string;
    fixes: FixRecord[];
}

// 修正引擎的输出
export interface AutoFixResult {
    fixedSections: SectionIconInfo[];  // 修正后的数据
    fixLog: SectionFixLog[];           // 修正日志（用于调试）
    totalFixes: number;                // 总修正次数
}
```

### 2.3 修正引擎主函数

```typescript
/**
 * 图标自动修正引擎
 * 
 * 在解析 Markdown 后、渲染前执行。
 * 负责保证同一个 Section 内所有卡片的图标使用一致。
 * 
 * 修正策略：
 * 1. 全部卡片无图标 → 保持（不强制添加）
 * 2. 部分有/部分无 → 取众数图标补齐
 * 3. 全部有图标但部分无效 → 替换为众数有效图标
 * 4. 全部有图标且全部有效 → 保持（AI 的语义选择正确）
 * 5. 全部有图标但全部无效 → 清空所有图标（好过全部显示错误的）
 */
export function autoFixIcons(sections: SectionIconInfo[]): AutoFixResult {
    const fixLog: SectionFixLog[] = [];
    let totalFixes = 0;

    const fixedSections = sections.map(section => {
        const sectionFixLog: SectionFixLog = {
            sectionTitle: section.sectionTitle,
            fixes: []
        };

        const { cards } = section;

        // Step 1: 校验每张卡片的图标有效性
        cards.forEach(card => {
            card.isValid = card.iconName !== null && iconExists(card.iconName);
        });

        // Step 2: 统计
        const hasIconCards = cards.filter(c => c.iconName !== null);
        const noIconCards = cards.filter(c => c.iconName === null);
        const validIconCards = hasIconCards.filter(c => c.isValid);
        const invalidIconCards = hasIconCards.filter(c => !c.isValid);
        const hasAnyValidIcon = validIconCards.length > 0;

        // ===== 分支判断 =====

        // 情况1：全部无图标 → 保持
        if (hasIconCards.length === 0) {
            return { ...section };
        }

        // 情况2：部分有图标、部分没有
        if (hasIconCards.length > 0 && noIconCards.length > 0) {
            if (hasAnyValidIcon) {
                // 有有效的图标 → 取众数补齐
                const mostUsedIcon = getMostUsedIcon(
                    validIconCards.map(c => c.iconName!)
                );
                
                noIconCards.forEach(card => {
                    sectionFixLog.fixes.push({
                        cardIndex: card.blockIndex,
                        from: null,
                        to: mostUsedIcon,
                        reason: '图标一致化：无图标卡片自动分配'
                    });
                    card.iconName = mostUsedIcon;
                    card.isValid = true;
                    totalFixes++;
                });
            } else {
                // 所有图标都无效 → 清空全部
                hasIconCards.forEach(card => {
                    sectionFixLog.fixes.push({
                        cardIndex: card.blockIndex,
                        from: card.iconName,
                        to: null,
                        reason: `所有图标无效（"${card.iconName}"不存在），已清空`
                    });
                    card.iconName = null;
                    card.isValid = false;
                    totalFixes++;
                });
            }
        }

        // 情况3：全部有图标，但部分无效
        if (hasIconCards.length > 0 && noIconCards.length === 0 && invalidIconCards.length > 0) {
            if (hasAnyValidIcon) {
                // 部分有效 → 用众数有效图标替换无效的
                const mostUsedIcon = getMostUsedIcon(
                    validIconCards.map(c => c.iconName!)
                );
                
                invalidIconCards.forEach(card => {
                    sectionFixLog.fixes.push({
                        cardIndex: card.blockIndex,
                        from: card.iconName,
                        to: mostUsedIcon,
                        reason: `图标 "${card.iconName}" 不存在，替换为 "${mostUsedIcon}"`
                    });
                    card.iconName = mostUsedIcon;
                    card.isValid = true;
                    totalFixes++;
                });
            } else {
                // 全部无效 → 清空
                cards.forEach(card => {
                    sectionFixLog.fixes.push({
                        cardIndex: card.blockIndex,
                        from: card.iconName,
                        to: null,
                        reason: `所有图标无效（"${card.iconName}"不存在），已清空`
                    });
                    card.iconName = null;
                    card.isValid = false;
                    totalFixes++;
                });
            }
        }

        // 情况4：全部有效 → 保持（无需任何操作）

        if (sectionFixLog.fixes.length > 0) {
            fixLog.push(sectionFixLog);
        }

        return { ...section, cards };
    });

    return {
        fixedSections,
        fixLog,
        totalFixes
    };
}

/**
 * 获取图标列表中出现次数最多的图标（众数）
 */
function getMostUsedIcon(icons: string[]): string {
    if (icons.length === 0) {
        // 兜底：返回默认图标
        return 'file-text';
    }
    
    const counts = new Map<string, number>();
    icons.forEach(icon => {
        counts.set(icon, (counts.get(icon) || 0) + 1);
    });
    
    return [...counts.entries()]
        .sort((a, b) => b[1] - a[1])[0][0];
}
```

### 2.4 降级默认图标定义

```typescript
/**
 * 当修正引擎无法确定合适的图标时的默认图标
 * 使用 file-text 是因为它在语义上最中性，适合大多数文档场景
 */
export const DEFAULT_FALLBACK_ICON = 'file-text';
```

### 2.5 修正引擎行为矩阵

| 输入状态 | 修正行为 | 修正后状态 |
|---------|---------|-----------|
| `[zap, cpu, null, null]` | 取众数 `zap` → 补齐 | `[zap, cpu, zap, zap]` |
| `[zap, zap, zap, null]` | 众数 `zap` → 补齐 | `[zap, zap, zap, zap]` |
| `[zap, brain, null, null]` | 众数（zap或brain 1:1 → 取第一个）→ 补齐 | `[zap, brain, zap, zap]` |
| `[zap, INVALID, cpu, null]` | INVALID → 众数 zap; null → zap | `[zap, zap, cpu, zap]` |
| `[INVALID_A, INVALID_B, null]` | 全部无效 → 清空 | `[null, null, null]` |
| `[zap, cpu, brain, server]` | 全部有效且全部有 → 保持 | `[zap, cpu, brain, server]` |
| `[null, null, null, null]` | 全部无图标 → 保持 | `[null, null, null, null]` |

---

## 三、步骤 2.2：在 CHDRenderer.tsx 中集成（0.5h）

### 3.1 集成点分析

**文件**: `MdToHtml/src/components/CHD/CHDRenderer.tsx`

**集成位置**：`useMemo` 中的 Section 构建完成后（约第225行），渲染之前。

### 3.2 具体修改

```typescript
// CHDRenderer.tsx — 在 useMemo 末尾增加修正引擎调用

// 当前代码（约第225行）：
//   return { sections: result, frontmatter: fm, error: null, blockMap: blockIndices };

// 修改后：
import { autoFixIcons, SectionIconInfo, CardIconInfo } from '@/services/ai/IconAutoFixService';

// 在 useMemo 中，return 之前加入：
// 4. 图标自动修正（如果启用了图标修正）
if (process.env.NEXT_PUBLIC_ENABLE_ICON_AUTO_FIX !== 'false') {
    const iconSections: SectionIconInfo[] = result.map((section: any) => ({
        sectionTitle: section.title,
        cards: section.cards.map((card: any) => ({
            blockIndex: card.blockIndex,
            iconName: card.props.icon || null,
            isValid: false,  // 将由修正引擎校验
        }))
    }));
    
    const { fixedSections, fixLog, totalFixes } = autoFixIcons(iconSections);
    
    if (totalFixes > 0 && process.env.NODE_ENV === 'development') {
        console.warn(`[IconAutoFix] ${totalFixes} 个图标被自动修正:`, fixLog);
    }
    
    // 将修正后的图标名写回 result
    fixedSections.forEach((fixedSection, sectionIdx) => {
        if (sectionIdx < result.length) {
            fixedSection.cards.forEach((fixedCard, cardIdx) => {
                if (cardIdx < result[sectionIdx].cards.length) {
                    if (fixedCard.iconName) {
                        result[sectionIdx].cards[cardIdx].props.icon = fixedCard.iconName;
                    } else {
                        delete result[sectionIdx].cards[cardIdx].props.icon;
                    }
                }
            });
        }
    });
}

return { sections: result, frontmatter: fm, error: null, blockMap: blockIndices };
```

### 3.3 环境变量控制

在 `.env.local` 中加入控制开关：

```
# 图标自动修正引擎开关（默认开启）
NEXT_PUBLIC_ENABLE_ICON_AUTO_FIX=true
```

### 3.4 集成后的数据流

```
原始 markdown
    │
    ▼
parseCHDBlocks() → 解析为 blocks
    │
    ▼
build Sections → 提取 card.props.icon
    │
    ▼
【新增】autoFixIcons() → 校验 + 修正图标
    │
    ▼
修正后的 sections → 传递给渲染层
    │
    ▼
Section.tsx → Card.tsx → 渲染
```

---

## 四、与现有架构的兼容性

### 4.1 不会影响的功能

| 功能 | 原因 |
|------|------|
| 编辑模式中的手动图标修改 | 修正引擎只在解析 markdown 时执行，编辑修改后重新触发 useMemo |
| 用户手动指定不同的图标 | 全部有效且一致 → 不修正 |
| 历史旧文档 | 修正引擎对旧文档同样有效，不会破坏历史数据 |
| Section 的图标一致性校验 | 修正引擎执行后，Section 收到的数据已经是修正过的，校验通过 |

### 4.2 边界情况处理

| 情况 | 处理方式 |
|------|---------|
| 传入空 sections | 直接返回空结果 |
| 单张卡片的 Section | 不需要修正（单张卡片不存在"一致性问题"） |
| 全部卡片都有不同图标 | 全部有效 → 保持（AI 的语义选择） |
| 修正引擎抛出异常 | 捕获异常，回退到原始数据不修正 |

---

## 五、验收标准

| 验收项 | 测试方法 | 预期结果 |
|--------|---------|---------|
| 部分有/部分无 → 补齐 | 输入 `[zap, null, cpu]` | 输出 `[zap, zap, cpu]`，fixLog 有 1 条 |
| 无效图标 → 替换 | 输入 `[zap, INVALID, cpu]` | 输出 `[zap, zap, cpu]`，fixLog 有 1 条 |
| 全部有效 → 保持 | 输入 `[zap, cpu, brain]` | 输出 `[zap, cpu, brain]`，fixLog 为空 |
| 全部无效 → 清空 | 输入 `[INVALID_A, INVALID_B]` | 输出 `[null, null]`，fixLog 有 2 条 |
| 全部无图标 → 保持 | 输入 `[null, null]` | 输出 `[null, null]`，fixLog 为空 |
| 修正日志正确性 | 检查修正前后的 iconName | from/to 严格对应修正前后状态 |
| CHDRenderer 集成 | 渲染带错误图标的 md 文档 | 图标被自动修正，无视觉不一致 |