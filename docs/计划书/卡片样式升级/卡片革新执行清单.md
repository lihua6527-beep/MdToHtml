# 卡片形态革新执行清单

本文档旨在明确“卡片形态”的实现位置及属性定义的具体层级。

## 1. 核心改动位置与层级

| 概念 (Concept) | 属性 (Prop) | 定义位置 (Definition Scope) | 作用对象 (Target) | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| **卡片形态 (Shape)** | `shape="{type}"` | **三级标题 (H3)** | 当前 H3 卡片本身 | 定义单个卡片的视觉外观。 |

### 示例代码结构

```markdown
### 极速渲染 {shape="cut"}
<!-- 这里的 shape="cut" 决定了这个卡片是切角形态 -->
采用 Rust 编写的核心引擎...

### 智能纠错 {shape="floating"}
<!-- 这里的 shape="floating" 决定了这个卡片带有悬浮标签 -->
自动识别 Markdown 语法错误...
```

---

## 2. 执行修改清单 (Action Items)

### 2.1 文档与规范修正 (Documentation)

- [x] **`card_shape_prototype.html`**: 
  - 将 "破格悬浮 (Floating)" 移至 "基础卡片样式 (Base Styles)" 区域。
- [x] **`卡片形态革新计划.md`**:
  - 更新 "抽象定义" 章节，明确 H3 定义 Shape。
  - 将 `floating` 正式列为一种 Shape。

### 2.2 代码实现逻辑 (Implementation Logic)

#### A. 解析器层 (Parser)
1.  **提取 H3 属性**: 解析 `### Title {shape="..."}`，将 `shape` 存入 Card 对象。

#### B. 渲染层 (Renderer - React Component)
1.  **Card 组件**:
    - 接收 `shape` 属性。
    - 根据 `shape` 渲染对应的 CSS 类名 (e.g., `.shape-cut`, `.float-badge`)。
    - **破格悬浮 (Floating)** 实现：
      - 渲染标准卡片结构。
      - 额外渲染一个绝对定位的 `.float-badge` 元素（内容可来源于 H3 的元数据或约定）。

---

## 3. 预览与验证 (Verification)

在开发完成后，需通过以下测试用例验证：

1.  **Case 1: 自由组合**
    - H3 A: `shape="cut"` -> 应显示切角卡片
    - H3 B: `shape="floating"` -> 应显示悬浮标签卡片

---

*Last Updated: 2026-02-24*
