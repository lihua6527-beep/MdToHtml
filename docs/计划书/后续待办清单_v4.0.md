# 后续待办清单 (Next Phase Backlog) - v4.0

**关联 ADR**: ADR-003
**状态**: 待排期

## 1. P1 - 核心工程化与交付 (Engineering & Delivery)

| 任务ID | 任务名称 | 描述 | 验收条件 (AC) | 估时 | 责任人 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **ENG-01** | **离线构建适配** | 配置 Next.js `output: 'export'`，编写 `post-build` 脚本修复静态资源路径（图片、CSS）。 | 1. `npm run build` 生成 `out` 目录。<br>2. 直接打开 `out/index.html` 无 404 错误。<br>3. 图片资源正常显示。 | 4h | 架构师 |
| **ENG-02** | **自动化测试基座** | 搭建 Jest + React Testing Library 环境，并编写 `chdParser.ts` 的核心单元测试。 | 1. `npm run test` 可运行。<br>2. 解析器核心逻辑覆盖率 > 80%。 | 4h | 架构师 |

## 2. P2 - 健壮性与安全 (Robustness & Security)

| 任务ID | 任务名称 | 描述 | 验收条件 (AC) | 估时 | 责任人 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **ROB-01** | **本地存储防护** | 封装 `useAutoSave` 中的 localStorage 操作，增加 `try-catch` 和容量检测。 | 1. 模拟存储满时不出错。<br>2. 写入失败有控制台警告或 UI 提示。 | 2h | 开发者 |
| **ROB-02** | **404 页面完善** | 创建自定义 `not-found.tsx`，提供返回首页的引导。 | 1. 访问不存在路径显示友好 UI。<br>2. 包含"返回编辑器"按钮。 | 2h | 开发者 |

## 3. P3 - 深度体验优化 (Deep UX Optimization)

| 任务ID | 任务名称 | 描述 | 验收条件 (AC) | 估时 | 责任人 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **UX-01** | **全站 UI 标准化** | 将 `Dialog`, `Input`, `Select` 等原生组件全部替换为 `shadcn/ui` 组件。 | 1. 无原生 HTML 表单控件。<br>2. 视觉风格完全统一。 | 8h | 开发者 |
| **UX-02** | **暗色模式适配** | 完善 Tailwind Dark Mode 配置，确保所有组件支持日/夜切换。 | 1. 增加切换按钮。<br>2. 所有文字/背景对比度符合 WCAG AA。 | 4h | 开发者 |

## 4. P4 - 前沿性能探索 (Performance Exploration)

| 任务ID | 任务名称 | 描述 | 验收条件 (AC) | 估时 | 责任人 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **PERF-01** | **解析器 Worker 化** | 将 `chdParser` 移至 Web Worker 运行，释放主线程。 | 1. 大文档输入时 UI 帧率保持 60fps。<br>2. 解析耗时不阻塞打字。 | 16h | 架构师 |

---
**更新日志**:
*   2026-02-05: 初始化 v4.0 清单，基于《持续优化机制与架构治理报告》生成。
