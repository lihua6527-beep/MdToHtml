# 已完成功能清单

## 2026-02-26
- [x] **文档列表增强 (Document List Enhancements)**:
    - **排序优化**: 移至设置菜单，支持按导入时间/修改时间/访问时间排序。
    - **宽屏视图**: 顶部新增展开按钮，支持一键切换至半屏双列视图 (50% Width, 2 Columns)，优化长文件名显示。
    - **UI 精简**: 移除冗余的预览按钮与顶部排序开关，界面更清爽。
- [x] **Tags 样式增强与主题集成 (Tags & Theming)**:
    - **5种高级风格**: 实现 Glass, Tech, Gradient, Outline, Pop 五种渲染风格。
    - **智能图标映射**: 基于 Lucide Icons 实现标签内容到图标的语义化自动映射。
    - **主题集成**: 在底部工具栏新增 Tags 样式选择器，支持实时预览与切换。
    - **持久化**: 样式配置自动同步至 Frontmatter `tag-style` 字段。
- [x] **CHD 协议严格化 (Strict Enforcement)**:
    - **代码样式净化**: 在渲染层拦截 Inline Code 的滥用，非代码/箭头/引用内容的 Inline Code 自动降级为 **Bold** 样式，防止视觉污染。
    - **Tags 渲染修正**: 严格遵循 v2.3 协议，仅在文档顶部 Frontmatter 区域渲染 Tags，彻底移除 Card 内部的 Tags 显示。
- [x] **交互与导航优化 (Interactive & Navigation)**:
    - **文档回退**: 在编辑器顶部增加 `<ArrowLeft />` 返回主页按钮。
    - **主页精简**: 移除主页“进入编辑器”入口，强制基于现有文档工作。
    - **全局设置**: 将设置入口移至左下角，支持上拉菜单与侧边栏联动。
    - **布局优化**: 优化左侧 Tab 栏垂直空间利用与分割线视觉。
- [x] **编辑器体验修复 (Editor UX Fixes)**:
    - **全局布局配置 (Global Layout Config)**: 将分区间距与分割线开关升级为 Frontmatter 全局配置，实现“一键调整所有分区”。
    - **交互响应修复**: 修复了点击工具栏不触发页面刷新的 Bug，通过重构数据流确保 React 状态与 Markdown 属性实时同步。
    - **保存逻辑修正**: 区分手动保存（关闭编辑器）与自动保存（保持编辑状态），修复输入中断问题。
    - **启动脚本优化**: 修复 `start.bat` 重复打开浏览器窗口的问题，确保单实例启动。
    - **UI 细节打磨**: 修复底部工具栏分割线双线问题，统一视觉风格。
- [x] **逻辑与状态管理 (Logic & State)**:
    - **状态锁定**: 简化文档状态为 `incomplete` / `completed`。
    - **持久化交互**: 状态切换按钮常驻显示，点击即自动保存并更新 Frontmatter，无需进入编辑模式。
    - [x] **逻辑修正**: 移除了“已完成”状态下编辑自动重置为“未完成”的逻辑，确保状态锁定。
    - [x] **性能架构升级 (Performance Architecture)**:
    - **元数据缓存 (SYS-02)**: 引入 `MetadataCacheManager`，实现 O(1) 复杂度的文档列表加载，解决启动慢问题。
    - **Top 500 限制 (SYS-03)**: 默认缓存最近 500 个文档，超出部分自动归档，保证内存与性能稳定。
    - **状态一致性**: 通过缓存层收束状态管理，彻底修复了“已完成”文档状态回退的 Bug。
    - **乐观更新**: 状态切换按钮引入 Optimistic UI 与 Loading 动画，消除操作延迟感。

## 2026-02-25
- [x] **编辑器统一与重构 (Grand Unification)**:
    - **全权回归**: 将散落在页面中的主题、二级标题、卡片控制权全部收归至底部固定编辑器 (BottomToolbar)。
    - **体验优化**: 极大地减少了画面凌乱程度，符合用户认知规律，统一了操作入口。
    - **扩展性基石**: 建立策略模式 (Strategy Pattern)，后续扩展只需注册新策略，无需侵入核心代码。
- [x] **AI 降维优化**:
    - **样式精简**: 将卡片样式从 7 种精简为 4 种 (Standard, Highlight, Quote, Code)，显著降低 AI 训练维度。
- [x] **协议锁定 (Protocol Locking)**:
    - **最小闭环**: 锁定 CHD 协议 v1.6，仅允许 **Grid 布局** + **矩形卡片**，禁用多边形/环形等复杂样式，确保 AI 生成的确定性。
- [x] **交互修复与调试**:
    - **深度 Debug**: 解决了底部工具栏无法获取分区列表 (Sections) 的关键 Bug（耗时 4h+，定位到 `InteractivePost` 未透传 props）。
    - **全局布局控制**: 迁移分区列数和间距控制至底部工具栏「主题」页，实现全局统一管理。
- [x] **分割线优化**: 实现全局分割线开关，优化分割线与卡片的间距 (mt-4 mb-4) 并确保视觉对称。
- [x] **视觉稳定性**: 移除分区选中时的布局抖动，采用绝对定位透明层。
- [x] **列数选项调整**: 移除「6列」选项，优化移动端/窄屏显示。


## 2026-02-24
- [x] **卡片样式标准化**: 精简为 Standard, Highlight, Quote, Code 四种核心样式。
- [x] **底部工具栏重构**: 
    - 统一 UI 文字大小 (14px/12px)。
    - 重组 Theme/Layout/Card 标签页结构。
    - 修复颜色切换跳转问题。
- [x] **自动保存机制**: 实现 5 秒防抖自动保存 (Silent Save)。
- [x] **属性解析优化**: 支持行尾未闭合的大括号解析。
- [x] **悬浮撤销/重做**: 引入 FloatingUndoRedo 组件。

## 历史完成
- [x] **基础编辑**: Markdown 实时预览、双向滚动同步。
- [x] **CHD 渲染引擎**: 支持 Grid/Timeline/Gallery 等多种布局。
- [x] **交互式组件**: 可拖拽排序、点击编辑、快捷键支持。
- [x] **多主题支持**: 内置 5 种莫兰迪配色主题。
- [x] **导出功能**: 支持导出为单文件 HTML (内联 CSS/JS)。
