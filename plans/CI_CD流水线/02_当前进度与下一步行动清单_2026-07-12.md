# Plan 02: 当前进度与下一步行动清单

> **更新日期**：2026-07-12  
> **关联计划**：CI/CD 流水线总体计划书 + 前端交互验证方案

---

## 已完成的工作

### ✅ E2E 测试体系（3/5 核心测试用例完成）

| 文件 | 测试场景 | 状态 |
|------|---------|------|
| `e2e/test-helpers.ts` | 测试辅助函数 | ✅ |
| `e2e/editor-workflow.spec.ts` | 新建→编辑→预览 + 增量编辑 | ✅ |
| `e2e/file-management.spec.ts` | 保存→刷新→持久化 + 删除→回收站→恢复 | ✅ |
| `e2e/search-and-export.spec.ts` | 搜索功能 + HTML 导出 | ✅ |
| `e2e/interactive/*.spec.ts` | 撤销/重做/AI生成/主题切换（计划中） | ⏳ |

### ✅ CI/CD 流水线

| 文件 | 用途 | 状态 |
|------|------|------|
| `.github/workflows/pr-check.yml` | PR 门禁：Lint + TypeCheck + UnitTest | ✅ |
| `.github/workflows/ci.yml` | 合并后 CI：Build + E2E | ✅ |
| `release.yml` | 发布流水线（参考计划书，可后续补充） | ⏳ |

### ✅ 基础配置

| 文件 | 用途 | 状态 |
|------|------|------|
| `playwright.config.ts` | Playwright 完整配置（含 webServer） | ✅ |
| `package.json` | 已添加 @playwright/test 依赖 | ✅ |
| Playwright Chromium | 浏览器安装（后台下载中 183MB） | ⏳ |

---

## 待完成的工作

### 高优先级

- [ ] 等待 Chromium 下载完成 → 运行 `npx playwright test` 验证 E2E 测试通过
- [ ] 修复任何失败的测试用例
- [ ] 运行 `npx playwright show-report` 查看测试报告
- [ ] 将项目推送到 GitHub，验证 CI/CD 流水线触发
- [ ] 在 README 顶部添加 CI 状态徽章

### 中优先级

- [ ] 补充 2 个交互测试（撤销/重做 + 主题切换）
- [ ] 配置 Jest 覆盖率阈值（jest.config.js 中增加 coverageThreshold）
- [ ] 配置 Codecov 集成（需注册 Codecov 账号）

### 低优先级

- [ ] 创建 release.yml 发布流水线
- [ ] Electron 打包自动化

---

## 验证步骤

```bash
# 1. 运行 E2E 测试
cd MdToHtml
npx playwright test

# 2. 查看测试报告
npx playwright show-report

# 3. 运行单元测试（含覆盖率）
npm run test -- --coverage --ci

# 4. TypeScript 类型检查
npx tsc --noEmit

# 5. Lint 检查
npm run lint -- --max-warnings 0

# 6. 生产构建
npm run build