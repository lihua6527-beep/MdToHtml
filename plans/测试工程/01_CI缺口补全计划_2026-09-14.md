# Plan 01: CI 缺口补全计划（P6 运维闭环 / P7 覆盖率爬坡 / P8 质量看板）

> **计划日期**：2026-09-14
> **计划目标**：在 P0–P5（测试与门禁修复，已完成）基础上，补齐"流水线真实验收 / 发布自动化 / 覆盖率爬坡 / 质量看板"四类缺口，使「自动化测试 + CI/CD」从"本地全绿"升级为"远端全绿且可展示"。
> **前置依据**：`docs/自动化测试与CI流水线_完成度分析报告_2026-09-14.md`（缺口清单 G1–G9）
> **上游计划**：`plans/测试工程/00_CI全绿计划书_2026-09-14.md`（P0–P5 已完成）、`docs/归档/计划书/CI_CD流水线/00_CI_CD流水线总体计划书_2026-07-12.md`（阶段三 / 阶段四设计稿，已归档）
> **执行手册**：`docs/开发工作流/SOP_测试与CI门禁规范.md`
> **状态**：⏳ 未开始（本文档为计划，不含执行结果）

---

## 一、起点基线（2026-09-14 实测）

| 项 | 现状 |
|----|------|
| 单元测试 | ✅ 25 套件 / 213 用例全通过（6.0s） |
| E2E | ✅ 3 spec / 7 用例全通过（22.3s），数据隔离有效 |
| 覆盖率（全量 src） | 20.5 / 15.82 / 14.03 / 21.25，阈值 19 / 14 / 13 / 20 |
| CI 真实运行 | ❌ **从未触发**（`git remote -v` 为空；workflow 仅监听 `master`） |
| 发布流水线 | ❌ `release.yml` 不存在（仅 `npm run build:electron` 手动可用） |
| 质量看板 | ❌ 无 Codecov / 无徽章 / 无失败通知 |

### 覆盖的缺口

| 缺口 | 归属 |
|------|------|
| G1 远端/CI 未验收 | P6（✅ 2026-09-14 已解决：Gitee 流水线已启用并跑通） |
| G5 无发布流水线 | P6 |
| G2 覆盖率距目标差距 | P7 |
| G4 `cache-manager` 无测试 | P7 |
| G6 无质量看板 | P8 |
| G7 E2E 依赖中文文案 | P8 |

> G3（文档失真）、G8（工作区残留）、G9（历史遗留测试）已在 2026-09-14 的报告 / 手册 / 开发记录中处理或如实登记，不在本计划范围（G8 待用户裁决）。

---

## 二、P6 — 运维闭环（remote + push 验收 + 发布自动化）

**目标**：让远端 CI（本项目实际平台为 **Gitee 流水线**）真实运行并全绿，且一次打 tag 能自动产出便携版 exe。

### 6.0 远端现状与平台适配（2026-09-14 实测，重要）

已为本地仓库配置远端：`origin = https://gitee.com/njustzjh/md-to-html.git`（凭据可用，`git fetch origin --prune` 成功拉取全部分支）。

| 发现 | 说明 |
|------|------|
| **平台不匹配（阻塞项）** | 远端是 **Gitee**，而 `.github/workflows/*.yml` 是 **GitHub Actions** —— 在 Gitee 上**永远不会执行**。仅"配置 remote + push"并不能让 CI 变绿 |
| **分支分叉 2 / 2** | 远端 `feat/exe-package-ready` 有 2 个本地没有的提交（`d249e43` 07-26 工作区快照、`01bc1ce` README 克隆说明）；本地有 2 个远端没有的提交（`a0db033`、`424be17`，含全部 09-14 测试与门禁修复）。直接 push 会被拒（非快进），**禁止 force push** |
| **远端快照落后** | `git diff a0db033 d249e43` = 47 个文件差异：远端**不含** `config.e2e.json`（已删除）、仍是旧版嵌套 `.github/workflows/ci.yml`、`jest.config.js` 为旧口径、缺 `route.test.ts` / `useSearch.test.tsx` / `OperationBuilder.test.ts` 等 09-14 新增用例 |

**平台决策（P6 前置）**：✅ **方案 B（Gitee 流水线）已落地并跑通**（2026-09-14）：

| 方案 | 状态 |
|:----:|------|
| **A** GitHub 镜像跑 Actions | 未采用（保留为备选） |
| **B** Gitee 流水线（`.workflow/`） | ✅ **已采纳且已跑通**：`.workflow/流水线-202609142049.yml`、`.workflow/流水线-202609142107.yml`；schema 与启用步骤见 `docs/开发工作流/Gitee流水线配置指南.md`（§二 步骤 / §五 schema / §六 自检） |
| **C** 仅本地门禁 + Gitee 备份 | 未采用（兜底） |

**落地过程的关键结论（避免后人重踩）**：Gitee 流水线 YAML 与 GitHub Actions **完全不通用** ——
① `version: "1.0"` 必填；② stage 为**扁平**写法（`stages[i].steps`，不要 `stage:` 包裹）；③ step 是**插件 id**（`<动作>@<工具>`，如 `ut@maven` / `build@maven` / `build@nodejs`），**不存在 `shell@1`**；④ 命令放在 **`commands:`** 列表（不是 `inputs.run`）；⑤ 触发用 `triggers.push.branches.prefix`（**前缀匹配**）。

> **分叉处置（2026-09-14 已执行）**：采用 `git merge origin/feat/exe-package-ready`（合并提交 `10f9e2b`），8 处冲突全部按"本地已验收版本"解析，远端提交 `d249e43` / `01bc1ce` 完整保留为祖先；远端已跟踪的 `.trash/演示报告_*(4)` 与根 `package-lock.json` 一并对齐入库；合并后本地门禁复验仍为 **EXITCODE=0（25 套件 / 213 用例）**。**全程未做 force push**。

### 6.1 建立 remote 并触发首次 CI（🔴 必做）—— 已完成部分见「状态」列

| ID | 任务 | 操作 | 验收 | 状态 |
|----|------|------|------|:----:|
| 6.1.1 | 配置远端 | `git remote add origin https://gitee.com/njustzjh/md-to-html.git` | `git remote -v` 两行输出 | ✅ 完成 |
| 6.1.2 | 厘清分叉 | `git rev-list --left-right --count origin/<b>...<b>` | 得出 2/2 分叉并比对内容差异（47 文件） | ✅ 完成 |
| 6.1.3 | 消除分叉并推送 | `git merge origin/feat/exe-package-ready` → 解决冲突 → `git push origin feat/exe-package-ready` | 远端分支包含本次全部成果且无 force push | ✅ 完成（`10f9e2b`） |
| 6.1.4 | 建立 Gitee 流水线 | 新增 `.workflow/ci.yml`（方案 B） | 文件入库 | ✅ 完成 |
| 6.1.5 | **在 Gitee 页面启用流水线** | 仓库 → 顶部 **「流水线」** → 打开 `流水线-202609142107` → 运行（分支 `master`） | 两阶段全绿 | ✅ **已启用并跑通**（2026-09-14 由用户执行；运行日志未归档，如需可补） |
| 6.1.6 | PR 演练 | 开一个改动源码的 PR（非纯 `.md`） | 流水线自动触发并绿 | ⏳ 可选（推送已能触发，PR 演练非必需） |
| 6.1.7 | 自检（本轮新增） | 三分支 SHA 一致 / YAML 结构断言 / 本地 `verify` + E2E / 文档过期表述清理 | 全部通过 | ✅ 完成（结论见配置指南 §六） |

> **注意触发条件差异**：GitHub 侧两个 workflow 都 `paths-ignore: docs/**, **.md`（纯文档提交不触发）；Gitee 侧 `.workflow/流水线-*.yml` **未做 paths 过滤**，任何推送都会跑门禁（含纯文档提交）—— 若希望一致，可在确认页面支持后补充忽略规则。

### 6.2 发布流水线 `release.yml`（🟡 建议）

**设计**（沿用 `docs/归档/计划书/CI_CD流水线/00_CI_CD流水线总体计划书_2026-07-12.md` 的阶段三设计稿，并按当前技术栈落地）：

```yaml
name: Release

on:
  push:
    tags: ['v*']          # 由 npm version / 手动 tag 触发
  workflow_dispatch:       # 支持手动触发

permissions:
  contents: write          # 创建 Release 需要

jobs:
  package:
    name: Package Portable EXE
    runs-on: windows-latest          # ★ 必须 Windows：electron-builder 打 Windows 包在 Linux 上需要 wine
    timeout-minutes: 30
    defaults:
      run:
        working-directory: MdToHtml
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: MdToHtml/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Verify gates              # 打包前再跑一次门禁，防止把红灯代码打成包
        run: npm run verify
      - name: Build Next.js
        run: npm run build
      - name: Build Portable EXE
        run: npm run build:electron     # electron-builder --win portable → release/
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: portable-exe
          path: MdToHtml/release/*.exe
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: MdToHtml/release/*.exe
          generate_release_notes: true
```

| ID | 任务 | 说明 |
|----|------|------|
| 6.2.1 | 新建 `.github/workflows/release.yml` | 内容如上（`runs-on: windows-latest`） |
| 6.2.2 | 确认打包配置 | `package.json` 的 `build` 段已就绪：`target: portable`、`asar: false`、`sign: false`、产物名 `MdToHtml Pro-Portable-1.0.0.exe`（`directories.output: release`） |
| 6.2.3 | 版本号规范 | 唯一来源 `MdToHtml/package.json` 的 `version`；发版用 `npm version patch|minor`（会自动 commit + tag，tag 再 push 即触发） |
| 6.2.4 | 本地预演 | `npm run build && npm run build:electron`，确认 `release/` 出现 exe 且能启动 |
| 6.2.5 | 首次真实发版 | `git push origin v1.0.0`，确认 Release 页面自动挂载 exe |

> 与既有规范一致：`docs/工程目录与版本规范.md` 第 3 节已规定"`release/` 只保留最新一次打包结果""交付前必须重新打包"；本流水线把这两条从"人工纪律"变为"自动约束"。`release/` 已在根 `.gitignore` 中，不会污染仓库。

---

## 三、P7 — 覆盖率爬坡（20.5% → 25% → 35% → 60%）

**原则**：先补 **P0 业务模块**，再谈数字；每完成一档即按 `floor(实测)−1` 抬升阈值（流程见 SOP §4.2）。

### 7.1 分档路线与任务

| 档位 | 目标（Stmts/Lines） | 主要任务 | 预估工时 |
|:----:|:------------------:|----------|:--------:|
| 第一档 | ≥ 23 | `cache-manager` 8 例 + `utils` 2 例 | 3–4h |
| 第二档 | ≥ 28 | `trash-manager` 6 例 + `services/core`（ApiClient / TransactionManager）6 例 | 4–5h |
| 第三档 | ≥ 35 | `services/ai`（AIService / PromptEngine）4 例 + `hooks/editor` 4 例 + `/api/*` 路由 6 例 | 5–6h |
| 第四档 | ≥ 60 | 按覆盖率报告逐目录补齐（`app/api/**` 与 `components/**` 是最大缺口） | 8–12h |

### 7.2 第一档任务明细（建议本次先做）

| ID | 文件 | 用例清单（8 例） |
|----|------|------------------|
| 7.1.1 | 新建 `src/lib/__tests__/cache-manager.test.ts` | ① 首次 `getAll()` 触发扫描并回填缓存；② 命中缓存时不重复扫描；③ `refresh()`/`invalidate()` 后重新扫描；④ 容量上限清理（`capacityLimit`）保留最新 N 条；⑤ 排序稳定性（mtime 降序）；⑥ 空目录返回 `[]` 且不抛异常；⑦ 扫描期间并发调用只触发一次（去重/单飞）；⑧ 文件被外部删除后缓存条目被剔除 |

> 依据：`docs/归档/计划书/集成测试/01_覆盖率分析与模块测试优先级排期_2026-07-12.md` 将 `lib/cache-manager.ts` 定为 **P0（8 用例）**，至今未实现（当前仅 23.66% 间接覆盖）。

| ID | 文件 | 用例清单（2 例） |
|----|------|------------------|
| 7.1.2 | 新建 `src/lib/__tests__/utils.test.ts` | ① 格式化 / 截断等函数的输入输出；② 边界输入（空串、超长、特殊字符） |

### 7.3 每档收尾动作

1. `npm run test:ci` → 记录实测四项数值；
2. 修改 `jest.config.js` 的 `coverageThreshold`（按公式），并更新注释里的基线数字；
3. 更新 `docs/自动化测试与CI流水线_完成度分析报告_2026-09-14.md` §4.2 与 `docs/开发工作流/SOP_测试与CI门禁规范.md` §4.2 的对照表；
4. 单独提交：`test(cache-manager): 补 8 例并抬升覆盖率阈值 19→22`。

---

## 四、P8 — 质量看板 + E2E 去中文文案

### 8.1 质量看板（G6）

| ID | 任务 | 具体内容 |
|----|------|----------|
| 8.1.1 | Codecov 上传 | `pr-check.yml` 的 Coverage 步骤后追加 `codecov/codecov-action@v4`（需 `CODECOV_TOKEN` secret，`directory: MdToHtml/coverage/`）；本地 `coverage/lcov-report` 已由 jest 生成，`lcov` reporter 若缺失需在 `jest.config.js` 补 `coverageReporters: ['text','lcov','json-summary']` |
| 8.1.2 | README 徽章 | 顶部添加 CI 状态徽章与覆盖率徽章（需替换为真实 `owner/repo`）：`![CI](https://github.com/<owner>/<repo>/actions/workflows/ci.yml/badge.svg)` |
| 8.1.3 | 失败通知 | `ci.yml` 末尾追加 `if: failure()` 的 `actions/github-script@v7` 步骤，在 PR 下评论失败链接（模板见 `docs/归档/计划书/CI_CD流水线/00_CI_CD流水线总体计划书_2026-07-12.md` §6.2 Step 3） |
| 8.1.4 | 覆盖率趋势 | 依赖 8.1.1；确认 Codecov 面板出现 4 项曲线 |

**前置依赖**：GitHub 仓库地址（同 6.1.1）；Codecov 账号与 token（8.1.1）。

### 8.2 E2E 去中文文案依赖（G7）

当前脆弱点（文案一改即静默失效）：

| 用例位置 | 现用选择器 | 建议改造 |
|----------|-----------|----------|
| `e2e/test-helpers.ts:110` | `getByRole('button', { name: '编辑页面' })` | 组件补 `data-testid="enter-edit"` |
| `e2e/test-helpers.ts:111` | `getByRole('button', { name: '保存修改' })` | 组件补 `data-testid="save-doc"` |
| `editor-workflow.spec.ts:36,39` | `getByRole('button', { name: '取消/预览' })` | 组件补 `data-testid="cancel-edit"` |
| `search-and-export.spec.ts:26` | `button[title="搜索文档"]` | 补 `data-testid="open-search"` |
| `search-and-export.spec.ts:48` | `button[title="导出为静态网页 (HTML)"]` | 补 `data-testid="export-html"` |

| ID | 任务 | 说明 |
|----|------|------|
| 8.2.1 | 在生产组件上补 `data-testid` | 先定位真实渲染组件（顶部栏来自 `InteractivePost.tsx` / `NavigationHeader` 链路，导出按钮来自 `ExportButton.tsx`；注意现有同名 testid 只存在于测试 mock 中） |
| 8.2.2 | 更新 E2E 选择器 | 改用 `getByTestId`，并保留旧文案选择器作为注释里的"曾经用法"便于回溯 |
| 8.2.3 | 补交互型用例（可选） | `e2e/interactive/undo-redo.spec.ts`（撤销 / 重做）、主题切换 2 例（`docs/归档/计划书/集成测试/01_*` 的遗留项） |
| 8.2.4 | 更新约定文档 | `MdToHtml/tests/README.md` §稳定选择器约定 + SOP §5.2 表格同步 |

---

## 五、验收标准

| 阶段 | 验收命令 / 观察点 | 通过标准 |
|:----:|------------------|----------|
| P6 | GitHub Actions 页面 | `CI Pipeline`（build + e2e 两个 job）与 `PR Check` 均出现绿色运行记录；至少一次 PR 演练通过 |
| P6 | 打一次 tag | Release 页面自动挂载 `MdToHtml Pro-Portable-<version>.exe`，本地下载可启动 |
| P7 | `npm run test:ci` | 覆盖率较基线提升且门禁通过；每档阈值已按公式抬升并单独提交 |
| P7 | `src/lib/__tests__/cache-manager.test.ts` | 存在且 8 例全通过（补上 P0 模块的历史欠账） |
| P8 | README / Codecov / 通知 | README 有可点击的 CI 与覆盖率徽章；Codecov 有曲线；故意制造一次失败能在 PR 看到通知 |
| P8 | `npx playwright test` | 7+ 用例全通过，且**不再依赖任何中文按钮文案** |

---

## 六、风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 首次 CI 中 E2E 时序不稳定（本地 3 workers，CI 1 worker + retries 2） | build job 绿、e2e job 红 | 保留 `retries: 2` + `trace: on-first-retry`；失败先看 artifact 里的报告与截图再改断言，**不要**把 `continue-on-error` 加回去 |
| `npm ci` 与 `package-lock.json` 不一致 | 首个 job 即失败 | 提交前本地跑一次 `npm ci` 验证；注意仓库根目录那个 **87 字节的空壳 `package-lock.json`** 不参与 CI（CI 用的是 `MdToHtml/package-lock.json`），但建议按 G8 裁决清理以免误导 |
| electron-builder 在 Linux runner 上打 Windows 包失败 | 发布流水线红 | 已规避：`release.yml` 使用 `runs-on: windows-latest` |
| 打包体积 / 时间超限 | 30min 超时 | 已设 `timeout-minutes: 30`；必要时先上传 artifact 再创建 Release |
| Codecov 需要额外账号与 token | 8.1 阻塞 | 可先做 8.1.2 徽章（CI 徽章不需要 token），Codecov 延后 |
| 阈值抬升触发"为了过门禁而改口径"的诱惑 | 门禁失去意义 | SOP §4.2 明确禁止；阈值改动必须单独提交并在提交信息中写明 x→y |

---

## 七、执行顺序与工时汇总

```
① P6.1 remote + push 验收         1–2h   ← 唯一能证明"CI 可用"的动作，先做
② P7.1 第一档（cache-manager + utils） 3–4h   ← 补 P0 模块欠账，性价比最高
③ P6.2 release.yml + 首次发版      2–3h
④ P8.2 E2E 去中文文案              2h
⑤ P8.1 质量看板（徽章 → Codecov → 通知） 1–2h
⑥ P7 第二/三档（覆盖率 28% → 35%） 9–11h
⑦ P7 第四档（→ 60%）               8–12h   ← 长期项，可与其他开发任务并行推进
```

**关键路径**：① → ③（release.yml 依赖 remote 才能验收）。

---

## 八、范围外（明确不做）

| 项 | 原因 |
|----|------|
| 视觉回归测试（截图比对基线） | 当前 UI 迭代频繁，基线维护成本高于收益；先用 E2E 保功能正确性 |
| 把覆盖率门禁直接设为 60% 阻断 | 会导致 CI 长期红灯，门禁失效；按 §7.1 分档抬升 |
| 归档 / 删除 `tests/*.py` 与 `src/app/test/page.tsx`（G9） | 需人工确认；`MdToHtml/tests/README.md` 已标注"不在 CI 中运行" |
| 处理工作区残留（G8：根 87B `package-lock.json`、`plans/审计与`、`.trash/演示报告_*.md`） | 待用户裁决，本计划不改动 |

---

> **收尾约定**：P6–P8 任一阶段完成后，按 `.cursorrules` 第 6 条归档本计划书至 `docs/归档/计划书/`，并更新 `docs/开发记录/历史记录/[日期]_开发记录.md` 与 `docs/自动化测试与CI流水线_完成度分析报告_2026-09-14.md` 中的完成度矩阵。
