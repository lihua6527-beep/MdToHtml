# MdToHtml Pro —— 演示报告（叙事答辩版）

> **产品名称**：MdToHtml Pro — 本地离线的 Markdown 卡片式文档工程系统
> **一句话定位**：让"AI 只负责写规范文本、程序负责确定性排版、文档随时随地可带走"的文档生产系统
> **版本基线**：v1.0.0 ｜ CHD 协议 v2.1（2026-09-07 生效）｜ 报告整理于 2026-09-08
> **技术栈**：Next.js 14 · React 18 · TypeScript · Tailwind CSS · CodeMirror 6 · Electron 28
> **适用场合**：毕业答辩演示 / 项目路演 / 评委问答（建议时长 12~15 分钟，附[现场演示脚本](#6-现场演示脚本12-分钟对照版)）

> 📌 **本报告与旧版 `演示报告.md` 的关系**
> 旧版是"8 大亮点 + 数据表"的产品说明书写法；本报告改为 **"一条叙事主线 + 六场拆解"** 的答辩式写法——
> 顺着「为什么做 → 设计上如何破局（三大突破）→ 交给 AI 如何保证可信 → 呈现上如何顺畅 → 工程上如何可靠 → 现场怎么讲」展开，
> 让每一章都有"论点—论据—数据—演示动作"四件套。旧版的数据表与竞品对比仍可直接复用（见[附录 A](#附录)）。

## 目录

1. [开场：我们在解决什么问题](#1-开场我们在解决什么问题1-分钟)
2. [设计层面的三大突破](#2-设计层面的三大突破4-分钟)
3. [AI 接入的工程可信：安全性 · 可靠性 · 有限性](#3-ai-接入的工程可信安全性--可靠性--有限性3-分钟)
4. [渲染与交互：把"效果"做到可感知](#4-渲染与交互把效果做到可感知3-分钟)
5. [系统与体验工程：让软件"好用、不打扰"](#5-系统与体验工程让软件好用不打扰3-分钟)
6. [现场演示脚本（12 分钟对照版）](#6-现场演示脚本12-分钟对照版)
7. [附录：数据速查 · 口径校准 · Q&A 预演](#附录)

---

## 1. 开场：我们在解决什么问题（1 分钟）

**讲一个用户故事开场（30 秒）**：

> 你是一名研究生 / 技术作者：论文要排版，技术文档要分层、突出重点。用 Word 排半天，用 Typora 写出来却是一长条"会呼吸的白纸"；
> 让 AI 帮你排版，它却给你一堆样式失控、无法维护、换个主题就崩的 HTML；好不容易写完，导出不顺手、分享出去样式全丢、改错了想撤回却无能为力。

**给出本系统的答案（30 秒）**：

| 用户的痛 | 我们的解 |
|---|---|
| 长文平面化，看不出结构与重点 | 把文档组织成「章节 → 卡片」，自动排版成卡片式网格 |
| 让 AI 排版 = 灾难 | AI 只生成受约束的**纯文本**，排版交给**确定性规则引擎** |
| 内容被锁死在某个软件里 | 数据是普通 Markdown，导出是独立 HTML 单文件，双击即用、随处分发 |
| 工具要联网、要装"全家桶" | 本地离线优先，18MB 便携版即点即用，仅 AI 生成按需联网 |

> **过渡句**：要做到上面四点，我在**设计层**做了三件事——这就是今天要讲的"三大突破"。
---

## 2. 设计层面的三大突破（4 分钟）

三大突破共同回答一个问题：**如何让"内容生产"和"视觉排版"都变得确定、可控、可维护？**

| # | 突破 | 一句话 | 解决的核心矛盾 |
|---|------|--------|---------------|
| 一 | **AI 只生成文本** | 内容归 AI、排版归程序，双方不跨界 | AI 输出的不确定性 vs 排版必须 100% 可控 |
| 二 | **自研 CHD 协议** | 用一份"三层刚性结构"约束一切文档 | 通用 Markdown 的自由散漫 vs 工程化文档需要结构 |
| 三 | **线性书写 · 展开呈现** | 源码保持一维线性，渲染端把它"展开"成二维网格 | 人/程序都好写的一维文本 vs 读者需要的高密度二维排版 |

### 2.1 突破一：AI 只生成文本（内容与排版彻底分离）

**设计决策**：让 AI 直接输出 HTML、或输出带绝对样式的文档，看似省事，实则埋下三颗雷：

1. **样式失控** —— AI 每次生成的类名、结构都不一样，同篇内容换个主题就崩；
2. **不可维护** —— HTML 混进内容后 diff 无法看、改稿无从下手、无法二次结构化；
3. **风险放大** —— AI 生成的 HTML/脚本一旦进入渲染与导出链路，注入攻击面也随之扩大。

**CHD 的答案，是把"二维布局问题"降维成一维的"内容类型标签"序列**：

- 人和 AI 都只写**规范 Markdown 文本**：标题、列表、表格、公式、代码块；
- 排版所需信息收敛成少数几个**扁平的语义属性**（`card-style` / `card-color` / `columns` / `badge` / `shape`…）；
- 真正的排版（几列、几行、什么颜色、什么形状）由**确定性规则引擎**统一决定，人和 AI 都无法"越权"写样式。

```
传统路线：用户 / AI ──写样式──▶ HTML（每次都不一样，不可控）
本项目：  用户 / AI ──写规范文本 + 语义标签──▶ CHD Markdown ──▶ 确定性规则引擎 ──▶ 卡片网格
```

**收益（答辩话术）**："把不确定性留在文本层、把确定性留在渲染层"之后——

- AI 生成结果实测完整率 **100%**（协议约束 + Prompt 自检清单双保险，见 §3）；
- 换主题、换卡片风格、重新导出，**都不需要改动内容本身**；
- 渲染层成为可回归的纯函数（DFA 解析 O(n)），排版效果可批量验证、可写测试。

### 2.2 突破二：自研 CHD 协议（Card-based Hierarchical Document）

CHD 不是把 Markdown 魔改成新语言，而是在标准 Markdown 之上增加**三层刚性结构 + 一套语义属性**，让人、编辑器、AI 三方拥有同一个坐标系。

**三级刚性结构（铁律：禁止越级、禁止游离文本、禁止 `####` 或更深标题）**：

| 层级 | 语法 | 职责 |
|------|------|------|
| L0 | YAML Frontmatter | 文档元信息：`title / subtitle / tags` 必填，`date / author / summary` 选填 |
| L1 | `## 章节标题 {layout="grid" columns=N ...}` | Section 章节容器：负责横向切分 + 列数规则 |
| L2 | `### 卡片标题 {card-style="normal\|highlight\|quote" ...}` | Card 内容原子单元 |

**刻意收窄的属性面（防止人与 AI 的"选择困难症"）**：

- **L1 属性**：`layout`（固定 grid）、`columns`（按**对称布局规则**取值，禁止孤立行）、`section-color`（chart-1~5）、`title-spacing`、`show-divider`；
- **L2 属性**：`card-style`（AI 生成仅限 normal / highlight / quote 三种）、`card-color`、`badge`、`shape`（rectangle / cut-corner / arrow / floating / rounded）、`col-span / row-span`；
- **富文本**：LaTeX 公式、GFM 表格、代码块原生支持（学术场景刚需）。

```markdown
---
title: "本周架构周报"
subtitle: "聚焦首屏优化"
tags: ["Next.js", "性能"]
---
## 关键结论 {layout="grid" columns=3 section-color="chart-1"}
### 首屏从 1.85s 降到 280ms {card-style="highlight" card-color="chart-3" badge="核心"}
通过 SSR + 缓存预热 + 惰性编译的组合策略达成。
### 方案取舍 {card-style="normal"}
砍掉 PDF/PPT 导出，聚焦独立 HTML 单文件，守住质量下限。
```
### 2.3 突破三：线性书写 · 展开式二维呈现

**为什么"源文本必须保持线性"？**
写作场景里，内容天然是"一段话接一段话"的一维流；只有一维，键盘输入、Git diff、AI 生成、程序处理才都简单。传统排版软件让你直接"拖画布"，本质是把二维复杂度提前压给了作者。

**我们的做法 —— 把"二维化"从作者的负担变成引擎的服务**：

- **源码侧**：只看到一维 Markdown —— 易写、易审、易比对；
- **渲染侧**：以 Section 为容器，把其下卡片按**对称布局规则展开成网格** —— 卡片数与列数自动匹配（1→1、2→2、3→3、4→2、6→2/3、8→2/4、9→3、10+→3），**杜绝"一行 3 个 + 孤零零 1 个"的破碎版式**；
- **数量预警**：内容膨胀到 5 / 7 张时，协议建议拆成「2+3」「3+4」两个 Section，从源头保证版面永远对称。

**一句话演示话术**：同一份线性 Markdown，我把它交给规则引擎，"啪"地展开成一张卡片矩阵；列数、配色、徽章、跨列全由属性决定——**改属性就是改版面，改文字不动版面**。

**支撑这套体系的心脏 —— 手写 DFA 状态机解析器（O(n) 线性复杂度，非正则堆叠）**：

| 文档规模 | 优化前 | 优化后 | 提升 |
|---|---|---|---|
| 52 行小文档 | 2.1ms | **0.3ms** | 7.0× |
| 5,632 行超大型 | 89.0ms | **12.1ms** | 7.4× |

再叠加**分片缓存增量解析**（只重算被改动的内容分片），编辑体验做到"无感"：

| 编辑动作 | 全量重解析 | 增量解析 | 提升 |
|---|---|---|---|
| 修改 1 行文字 | 42ms | **0.8ms** | 52.5× |
| 插入 5 行内容 | 42ms | 1.5ms | 28.0× |
| 删除 1 张卡片 | 42ms | 2.1ms | 20.0× |

**健壮性（面向"用户总会写出坏文档"的现实）**：

| 异常文档 | 处理策略 |
|---|---|
| 未闭合代码块（缺 ```） | 2,000 行后强制闭合，并标记错误提示，不吞噬后续内容 |
| 未闭合 Frontmatter | 转为普通文本处理，不卡死 |
| 20,000+ 行超大型文件 | 10,000 行截断 + `incomplete` 标记，防内存溢出 |
| 标题含特殊字符 | 用内容哈希做稳定 ID，增删行后引用不漂移 |
| 空文档 | 正常返回空结构 |

> **小结话术**：三大突破是一套组合拳 —— **CHD 定义什么是好文档，AI 只生成文本保证文档能被程序读懂，线性书写 + 展开呈现让"写作"与"阅读"双向顺滑**。三者缺一，其余效果都会打折。
---

## 3. AI 接入的工程可信：安全性 · 可靠性 · 有限性（3 分钟）

> 答辩里 AI 部分最容易被评委追问三连：**你的 API Key 安全吗？断网/报错怎么办？它生成错了谁负责？**
> 本节就是这三问的完整答案：**安全上做收口，可靠上做兜底，有限性上把边界讲清楚**。

### 3.1 安全性：Key 不出前端，本地代理收口，只传必要内容

**架构一句话**：浏览器永远不直连 DeepSeek，所有 AI 请求经本地 Next.js API Route（`/api/ai/generate`）代理转发。

```
浏览器（用户文本 + 模型名 + prompt 变体）
      │  fetch /api/ai/generate（请求体里没有 Key）
      ▼
Node 服务端  ApiKeyManager.get() ──▶ Authorization: Bearer ★（Key 只在此刻拼接）
      │        模型白名单 / 120s 超时 / 4096 token 封顶 / temperature=0.3
      ▼
DeepSeek Chat Completions
```

安全措施逐条可讲：

1. **Key 不在前端**：`ApiKeyManager` 在服务端读取密钥；任何前端 bundle、请求体、响应体都不携带、不返回 Key；
2. **分层存储 + 零 I/O 读取**：读取优先级「内存覆盖值 → localStorage 恢复值 → `process.env.DEEPSEEK_API_KEY`」；保存经 `/api/ai/config/save` 落盘 `.env.local` 并通过内存惰性覆盖**即时生效、重启不丢**，运行期读取零文件 I/O；
3. **格式即防线**：设置面板对 Key 做 `sk-` 前缀 + 长度实时校验，提供「测试连接」按钮，首次使用弹窗说明费用与隐私；
4. **最小化传输**：请求体只含 `text + model + systemPrompt`，不含路径、不含无关数据；除"AI 生成"这一次请求按需出网外，文档内容全程留在本机；
5. **输出收敛**：模型名白名单（`deepseek-chat` / `deepseek-reasoner`）、`temperature=0.3` 压低随机性、`max_tokens` 封顶，从参数上先"踩刹车"。

> **可诚实补充的边界**：为换取"改 Key 不用重启进程"，当前把 Key 同时写入了浏览器 localStorage——这是**便利换安全的取舍**。对安全级别要求更高的部署，可关闭前端持久化、仅保留 `.env.local` 服务端注入（作为演进项，见附录 D）。

### 3.2 可靠性：超时、重试、错误码、空响应，全部有兜底

| 风险 | 工程兜底 | 表现 / 实测 |
|---|---|---|
| 上游迟迟不返回 | `AbortController` 120s 超时中止 | 返回 504「请求超时，请重试或选择更简单的文档」 |
| 瞬时网络抖动 | 指数退避重试 3 次（AIService 层） | 间隔 1s → 2s → 4s，重试后成功率约 99% |
| 401 / 403 / 429 / 5xx | 服务端按状态码分类，前端映射中文提示 | 「Key 无效 / 余额不足 / 频率超限」逐条可读 |
| 返回内容为空 | 服务端空内容守卫 | 直接判失败并提示，不留空文档给下游 |
| 用户反悔 / 取消 | 透传 `AbortSignal` | 即刻中断并返回「已取消」，不浪费预算 |
| 结构跑偏 | Prompt 内嵌 CHD 自检清单 + 渲染端兼容映射 | 旧样式自动映射新样式；实测完整率 100% |

> **可靠性话术**：**宁可让用户看到一句准确的中文错误，也不要让他盯着转圈 30 秒后一脸懵**——错误码→提示的映射是产品级设计，而不只是 `catch` 一下。

### 3.3 有限性：把边界说清楚，比吹牛更赢得信任

| 边界 | 现状 | 策略 / 演进 |
|---|---|---|
| 模型供应商 | 仅 DeepSeek（chat / reasoner 双模型） | 服务层已抽象 PromptEngine + AIService，可扩展多供应商 |
| 网络依赖 | 仅"AI 生成"按需联网，其余功能完全离线 | 断网不影响写作、渲染、导出 |
| 生成粒度 | 单请求整包返回（`stream:false`），非打字机流式 | 流式 / 中止续传列为可选演进方向（见附录 B 口径校准） |
| 单次输出上限 | `max_tokens = 4096`，超长文无法一次生成 | 三级 Token 预算"先估算、再生成"；长文分块生成规划中 |
| 结果正确性 | LLM 输出本质是概率性的 | 协议约束 + 渲染兜底 + 人工校对三重防线，不宣称"AI 不会错" |
| 成本 | 消耗用户自有 Key 额度 | 生成前有预算估算与费用说明 |
| 并发 | 单机单人顺序处理 | 明确产品边界，不做并发转换 |
| Key 存储 | localStorage + .env.local 双份 | 高安全部署可关前端持久化（附录 D） |
---

## 4. 渲染与交互：把"效果"做到可感知（3 分钟）

> 三大突破解决"文档能不能变好看且可控"，本章解决"软件本身顺不顺手、成果带不带走"。
> 分三段讲：**界面风格怎么选（渲染）→ 编辑错了怎么救（操作引擎）→ 成果怎么带走（下载全流程）**。

### 4.1 渲染界面的风格选择：可换肤、可继承、可携带

**风格选择要做到"同一份内容，多种气质"**：

- **全局主题**：默认 / 暗黑等多主题一键切换（`ThemeSwitcher`），莫兰迪低饱和色系统一视觉基调，不刺眼、耐读；
- **章节级配色**：Section 可指定 `section-color`（chart-1 蓝紫 … chart-5 橙黄），学术风默认避开粉红 chart-2；
- **卡片级语义样式**：`normal`（说明）、`highlight`（核心观点 / 关键数据）、`quote`（引文），配合 `badge` 徽章与 `shape` 形态（直角 / 缺角 / 箭头 / 浮动 / 圆角）表达内容层次；
- **配置优先级**：文档级 Frontmatter > 系统级 `config.json` > 默认值 —— 改一行属性就是改全文气质；
- **所见即所得**：风格修改实时回写 Frontmatter，Markdown 是唯一真源，重开不丢失、可 diff、可版本管理。

**最容易被追问的点："导出后主题为什么还是对的？"**

HtmlBundler 在导出时**显式注入编辑器当前主题**，而不是依赖浏览器 `prefers-color-scheme` 自动探测。这是踩坑后的修复：旧实现会让"深色系统的用户 + 浅色主题的编辑器"导出成黑底白卡的高反差事故。现在的规则是：**屏幕上看什么样，导出就是什么样**。

### 4.2 操作引擎：Markdown 是唯一真源，存储与撤回都围绕它

**设计原则**：一切视觉状态都能回到 Markdown——折叠 / 间距 / 样式若需持久化，一律写回 Frontmatter 或 Block 属性，杜绝"编辑器里改了、文件里没改"的两张皮。

**撤回体系的三层保护**：

1. **自动保存（第一层：内容不丢）** —— 默认 3,000ms 防抖落盘；大型文档单次保存约 62ms 且异步执行，不阻塞输入；
2. **无限级撤销 / 重做（第二层：操作可回）** —— 撤销引擎从早期的"整篇文本快照 `string[]`"重构为**操作序列栈 `Operation[]`**：每次编辑都记录一个带 `invert()` 逆操作的命令对象，`Ctrl+Z / Ctrl+Y` 或右上角浮动撤销条均可一步步回溯，任何一步都能还原；
3. **会话与文件兜底（第三层：误删能救）** —— 删除先进回收站（软删除），容量超限有预警和一键清理；AI 临时结果在退出前弹出保存确认。

**性能数据（"操作级回退"到底贵不贵？）**：

| 50 步累积操作 | 耗时 |
|---|---|
| 普通文字输入（30 字符/步） | ~3ms |
| Block 级操作（插入卡片/步） | ~28ms |
| 500 行全选替换 | ~85ms |

> **演进说明（实事求是）**：操作历史的**跨会话持久化**与 300ms 批量合并正在推进中；当前刷新页面后撤回栈重建，但**内容不丢由自动保存保证**——这是"先保内容、再保历史"的取舍。

### 4.3 下载全流程：从"编辑成果"到"别人能打开的文件"，一路顺畅

**为什么"导出"在同类工具中最容易翻车？**
因为导出 = 渲染引擎 + 样式系统 + 资源管理**三件事同时搬家**。我们的做法是把导出收敛成一条**单一可信链路**：

```
编辑器「导出 HTML」/ AI 结果「下载 HTML」
  → HtmlBundler.bundle(markdown, title, theme)
      ① renderToStaticMarkup —— 渲染文档结构字符串
      ② getCleanCSS —— 隐藏 iframe 内抽取干净 CSS（主路径，规避主题切换残留）
      ③ template.ts —— doctype / meta / 样式 / 脚本全部内联
  → 自包含 HTML 单文件（无外链、无 Node 依赖）
  → Blob + URL.createObjectURL + <a download> 浏览器原生下载
  → 亦可「保存为正式文档」落 posts/，或经 save-export 同步到 output/
```

**顺畅的量化证明 —— 解析结果以 DOM 片段形式缓存复用，导出阶段零重复解析**：

| 文档规模 | 产物大小 | 导出耗时 |
|---|---|---|
| 52 行 | 18KB | 35ms |
| 423 行 | 57KB | **92ms** |
| 1,847 行 | 156KB | 210ms |
| 5,632 行 | 420KB | 580ms |

**面向"真实分发"的细节**：中文文件名原生支持；文档标签 / 日期等元数据注入导出文件；产物可双击打开、U 盘分发、静态托管——一份"会动"的卡片页随时带走。
---

## 5. 系统与体验工程：让软件"好用、不打扰"（3 分钟）

> 答辩演示的"体感分"往往不在功能多，而在**等待少、界面净、跳转顺、数据稳**。本章四件事对应四类体感：开屏（别让我等）、设置折叠（别让我乱）、页面通信（别让我断）、多级存储（别让我丢）。

### 5.1 开屏优化：把"白屏焦虑"逐段消灭

**冷启动演进数据**（本机实测，5 次取中位数）：

| 指标 | 优化前 | 优化后 | 提升 |
|---|---|---|---|
| FCP 首次内容绘制 | 1,850ms | **280ms** | ↓85% |
| LCP 最大内容绘制 | 2,300ms | 420ms | ↓82% |
| TTI 首次可交互 | 2,600ms | 480ms | ↓82% |
| 首包 JS | 485KB | 142KB | ↓71% |

**不是单一招，而是组合拳叠加出的协同效应**：

| 措施 | 独立效果（对照 1,850ms） |
|---|---|
| SSR 首屏渲染 | → 740ms |
| 惰性编译 + SVG 加载动画 | → 820ms |
| 元数据缓存预热（异步扫描，不阻塞首屏） | → 650ms |
| 代码分割 + 按需加载 | → 1,100ms |
| **四项组合叠加** | **→ 280ms** |

**桌面启动的时序设计（容易被追问）**：
Electron 打包后要先拉起本地 Next 服务（约 2~3s）再开窗，如果干等就是"假死"观感。方案是**独立加载窗口**：在 `port+1` 起一个极简 HTTP 服务器提供加载页 + `/status` 端点；`setup-port.js` 监听 stdout 的编译就绪信号置 `ready`；加载页轮询到 `ready` 立刻跳转主界面。这个方案是踩过两个错方案后才定型的——`file://` 直开被浏览器 CORS 拦截、图片探测法语义模糊无法区分"连接中/已就绪"——**"就绪"必须来自真正的编译信号，而不是猜测**。

### 5.2 页面设置折叠：把复杂度收进抽屉，界面始终清爽

三个可以在现场真实演示的"折叠"：

1. **全局设置按类分组**：设置面板以「文件管理 / 网页渲染 / AI 服务 / 协议配置」页签组织，每个类别内部再折叠展开——路径、容量、API Key、CHD 协议预览都"点开才出现"，避免满屏表单；
2. **底部工具栏 Theme / Layout / Card 三段式 Tab**：选中预览中的 Section 自动切到"布局"页，选中卡片自动切到"卡片"页——**该出现时才出现**，平时一行工具栏很干净；
3. **侧栏 / 文档列表可收可展**：列表可收起成窄图标栏（宽度 `max-width` 平滑过渡），可展开成 50vw 宽屏双列网格预览大图；设置入口同样折叠成小图标，悬停才浮出文字。

> **话术**：界面上的"少"是工程上的"多"——每次只呈现当前任务需要的控件，其余收进折叠层；这既降低认知负担，也让低分辨率窗口不至于挤爆。

### 5.3 页面间通信：多页面应用的"内容搬运工"

系统是多页面结构：`/`（首页：文档列表 + AI 工作区）、`/editor/[slug]`（编辑器）、`/preview` 与 `/preview/[slug]`（渲染预览）、`/ai-input` 等。内容在页面间搬运采用**"主路径 + 回退 + 事件解耦"**三层策略：

| 场景 | 手段 | 为什么这样设计 |
|---|---|---|
| 保存 / 预览后进入编辑器 | URL 路由参数：`/editor/{slug}`、`/preview/{slug}` | URL 可刷新、可直达、可被 E2E 测试断言 |
| AI 结果即时预览 | `localStorage` 写入 `ai_preview_content` + 文件名，`window.open` 新标签打开 `/preview` | 内容较大，避免 URL 超长；刷新仍可恢复 |
| "去设置里配 Key" 并返回 | `CustomEvent('open-settings', { detail: { type: 'ai' } })` | 跨组件解耦，配置完仍停留在原上下文，不丢现场 |
| 路径配置改动全站同步 | `CustomEvent('app-paths-updated')` | 文档列表 / 回收站 / 编辑器统一响应，免手动 F5 |
| 页面内回退前进 | `router.push / router.back` | 保留浏览器前进后退语义，符合用户心智 |

> **话术**：**能用 URL 表达的状态不塞内存，能用事件解耦的调用不穿透 props**——这是本地多页应用保持低耦合、可单独测试的关键。

### 5.4 文件多级存储：每一份数据都有"该在的地方"

"文件多级存储"回答的是四个现实问题：**写到一半断电了 / 误删了 / 磁盘满了 / 文件多了变卡了，数据去哪了、怎么办？**

| 层级 | 位置 | 存放内容 | 职责 |
|---|---|---|---|
| L0 编辑会话层 | 内存 / localStorage / sessionStorage | 编辑中状态、AI 临时结果、撤销栈、置顶收藏与排序 | 秒级响应、会话级恢复 |
| L1 源文档层（唯一真源） | `posts/` | 正式 `.md` 文档 | 一切渲染 / 导出的数据基座 |
| L2 导出产物层 | `output/` | 打包后的 `.html` + 可选 `.json` 元信息 | 即产即用、可一键清理 |
| L3 回收 + 缓存层 | `recycle/` + `.metadata_cache.json` | 软删除文档、LRU-500 元数据缓存 | 删错能救、读得快 |

**配套工程细节（可被追问的硬货）**：

- **路径全部可配置**：`config.json` + PathManager 统一管理 input / output / data / recycle / CHD 协议路径；改完路径立即派发 `app-paths-updated` 事件全局生效；
- **容量可视化**：容量进度条 + 超限警告 + 一键清理；600 篇溢出触发 LRU 淘汰时仍保持 83% 缓存命中；
- **缓存增量维护**：只对 mtime 变化的文件重读 Frontmatter，不做全量扫描；热启动命中 100%、读取 8ms/篇；
- **多级缓存同步**：回收站恢复等写操作完成后，服务端重建元数据缓存 + 前端双刷新（`router.refresh` + 列表重拉），保证用户感知到的状态"即刻一致"。
---

## 6. 现场演示脚本（12 分钟对照版）

演示路线与叙事主线一一对应；"要说的话"一栏可直接背诵，出现 bug 时用"兜底"。

| 时间 | 动作 | 要说的话 | 对应章节 | 兜底 |
|---|---|---|---|---|
| 0:00–1:00 | 双击便携版 exe / `start.bat` | 从双击到可交互控制在秒级，没有白屏空窗 | §5.1 开屏 | 预热数据文件，避开冷扫描 |
| 1:00–2:00 | 首页巡览：列表折叠/展开、设置分组 | 列表可收窄可展开；设置折叠成组，只给当前需要的 | §5.2 | 准备一张"挤爆"截图对比 |
| 2:00–4:00 | 新建文档，粘贴一段带公式的普通文本 | 源码是线性 Markdown（一维），预览已实时展开成卡片网格（二维）——线性书写 + 展开呈现 | §2.3 | 用已备好的样张直接展示 |
| 4:00–5:30 | 手写 CHD：改 columns / card-style / card-color / badge | 排版 100% 由属性 + 规则引擎决定，改属性不动文字；可打开协议配置页对照 | §2.2 | 现场只改 1~2 个属性，其余口述 |
| 5:30–6:30 | 删 5 张卡片 → Ctrl+Z 逐步还原；展示自动保存 | 操作级撤回而非整篇快照；自动保存兜底断电 | §4.2 | 若动画卡顿，改用小文档 |
| 6:30–8:30 | AI 生成：选学术模板 → 上传示例 → 生成 → 保存为正式文档 | 传的是论文正文，回来的是受 CHD 约束的 Markdown；Key 只在服务端，断网会自动重试 | §3 | 提前"测试连接"；失败就讲错误提示设计 |
| 8:30–10:00 | 预览新页 → 一键导出 HTML → 在资源管理器双击产物 | 导出 92ms 级；主题、卡片、公式全保留；拷走即用 | §4.3 | 预先把产物放桌面，直接展示 |
| 10:00–11:30 | 全文搜索 → 回收站删除/恢复 → 容量条 | 删除进回收站；恢复后列表立即刷新（多级缓存同步） | §5.4 | 删除用草稿文档，避免误删 |
| 11:30–12:00 | 收尾翻回 [三大突破表](#2-设计层面的三大突破4-分钟) | 复述三大突破 + 一句总结金句（附录 E） | §7 | — |

**演示前 Checklist**
- [ ] 用「测试连接」验证 API Key 可用（避免现场首次请求 401）；
- [ ] 准备学术模板示例文档（如 `MdToHtml/posts/` 下的论文阅读报告）；
- [ ] 预演导出文件在资源管理器中双击打开成功；
- [ ] 关闭系统通知、断掉无关后台下载，保证帧率；
- [ ] 备份一份样张文档，防止现场误删。

---

## 附录

### A. 数据速查表（可整表引用）

| 指标 | 优化前 | 优化后 | 提升 |
|---|---|---|---|
| 首屏 FCP | 1,850ms | 280ms | ↓85% |
| 首包 JS | 485KB | 142KB | ↓71% |
| 编辑全链路 | 10.0ms | 1.8ms | ↓82% |
| CHD 解析（52 / 5,632 行） | 2.1 / 89.0ms | 0.3 / 12.1ms | 7.0×~7.4× |
| 增量解析（改 1 行） | 42ms | 0.8ms | 52.5× |
| 热启动缓存命中 | 0% | 100%（8ms/篇） | — |
| HTML 导出（423 行） | — | 92ms / 57KB | 较 Typora 约快 2.5× |
| AI 生成（5 卡短文） | — | ~3.5s | 完整率 100% |
| 自动保存（大文档） | — | 62ms 不阻塞 | — |
| 撤销 50 步（全选替换） | — | 85ms | — |

### B. 口径校准（重要，防止演示"翻车"）

以下三点是核对代码后必须对齐的"口径"，与部分旧文档/旧话术不一致，请以本表为准：

1. **AI 不是流式**：`/api/ai/generate` 当前代码为 `stream: false`，单请求整包返回，**并非 SSE 打字机**。若演示稿保留"流式逐字"说法，请二选一：改代码开启 stream 分支，或把话术改成"整包返回 + 超时/重试兜底"；
2. **卡片样式收敛**：现网 CHD v2.1 中，AI 生成仅允许 `normal / highlight / quote` 三种 `card-style`；旧版 `stat / warning / summary / code` 与图标体系已下线（渲染端仅做向后兼容映射），演示文档请勿再提 icon / stat；
3. **列数规则更新**：Section 列数采用**对称布局规则（禁止孤立行）**，不是旧版"1~4 张自适应、5 张以上一律 3 列"——演示时按新规则讲（1/2/3/4/6/8/9/10+ 的对应列数）。

### C. Q&A 预演（高频追问）

**Q1：API Key 存 localStorage，是不是不安全？**
A：分两层答。传输上 Key 不出前端——所有请求经本地 `/api/ai/generate` 代理，Key 只在服务端拼接；存储上是"便利换安全"：为了改 Key 不重启进程才做前端持久化，并做了 `sk-` 格式校验、测试连接、费用与隐私弹窗说明。若部署环境对安全更敏感，可只保留 `.env.local` 服务端注入（演进项已列出）。

**Q2：为什么不用流式 / SSE？**
A：早期评估过流式；当前选整包返回是为了让"120s 超时、3 次重试、幂等判断"更简单可靠，避免半截流的中断恢复复杂度。体验上先用"加载动画 + 耗时预估"补偿；流式作为演进方向已写在 Roadmap。

**Q3：AI 生成错了/不合规怎么办？**
A：三层防线——① 协议把自由度收窄，Prompt 自带自检清单，完整率实测 100%；② 渲染端对旧/异常样式做兼容映射，坏文档也"降级可用"而非白屏；③ 结果保存前人工可预览、可对比、可放弃。我不会宣称 AI 不出错，而是保证"错也可控、可回退"。

**Q4：5,000 行以上文档编辑会不会卡？**
A：诚实说明——当前 5,000+ 行编辑帧率约 32fps，日常文档（≤1,000 行）稳定 55~60fps；卡顿瓶颈在 DOM 数量而非解析（解析已有增量优化）。虚拟化渲染（只渲染可视区卡片）已列入 Roadmap，目标是 5,000+ 行回到 60fps。

**Q5：CHD 是你自创的协议，凭什么说它好？**
A：好不在"新"，而在**对齐了 AI 能力边界**——它把排版自由度收窄到 AI 能稳定输出的范围（文本 + 少量语义属性），把 100% 确定性留给规则引擎；同时它是标准 Markdown 的超集，不锁定生态，导出仍是通用 HTML。数据上解析 O(n)、增量 52× 都是对"简单协议"的回报。
### D. Roadmap 与演进方向

| 方向 | 内容 | 状态 |
|---|---|---|
| AI 内联编辑 | 编辑器内选中文本 → 浮动工具栏指令 → AI 原地改写 | 🔜 实施中（相关组件已出现于代码库） |
| 操作引擎增强 | 操作历史跨会话持久化 + 300ms 防抖批量合并 | 🔜 实施中 |
| AI 上下文预算 | 基于三级 Token 预算的自动裁剪，降低长文生成成本 | 📋 规划 |
| 流式输出 | `/api/ai/generate` 开启 `stream` 分支，打字机式呈现 | 📋 规划（与现有超时/重试体系兼容设计） |
| 超大型文档 | 虚拟化渲染（只渲染可视区卡片），5,000+ 行 32fps → 60fps | 📋 规划 |
| 高安全部署 | 可选项：关闭前端 Key 持久化，仅服务端 `.env.local` 注入 | 📋 规划 |

### E. 结语（可直接背诵的收尾）

> "这份系统做的其实只有一件事：**把不靠谱的事推给流程，把确定性留给代码**。
> AI 写文本，但不写排版 —— 排版交给 CHD 与规则引擎；
> 人写线性 Markdown，但不操心二维 —— 展开交给渲染器；
> 软件替你处理等待、记忆、找回与带走 —— 你要做的只是把内容写好。
> 数据在本地、导出能带走、错了能撤回、重启不白屏——文档，第一次真正属于写它的人。"

---

## 配套文档索引

| 文档 | 内容 | 位置 |
|---|---|---|
| 本报告（叙事答辩版） | 三大突破 / AI 可信 / 交互体验 / 系统工程 / 演示脚本 | `演示报告_答辩叙事版.md`（当前文件） |
| 演示报告（数据版） | 8 大亮点速览、竞品对比、详细数据 | `演示报告.md` |
| CHD 协议规范 v2.1 | 三级结构、对称布局列数规则、A/B Prompt 原文 | `MdToHtml/CHD协议.md` |
| 系统设计说明书 | 架构与模块设计、技术选型论证 | `docs/毕业设计/系统设计说明书.md` |
| 性能测试报告 | 首屏/解析/编辑/缓存/导出/AI 实测数据 | `docs/毕业设计/性能测试报告.md` |
| API 接口手册 | 24+ 本地 API 端点 | `API接口手册.md` |

> 数据声明：除明确标注"演进/规划"的条目外，本报告性能数据均出自仓库内《性能测试报告》《系统设计说明书》等实测文档（测试环境 Windows 11 + i7-12700H + 16GB + NVMe SSD，每场景 5 次取中位数，可复现）。

*—— MdToHtml Pro 答辩演示报告（叙事版）· 2026-09-08*







